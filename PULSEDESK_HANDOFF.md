# PulseDesk: AI Handoff Document

> **⚡ 2026-06-22 (later) — BULLETPROOF INGESTION: admin replies no longer dropped, delete/edit handled, threads completed. Code changes UNCOMMITTED / NOT pushed; data backfill is LIVE.** Fixes the pitch's core promise ("no message ever dropped, every ticket shows the full thread"). Investigation + plan: `.claude/plans/nifty-watching-aurora.md`. Three phases, each gated (`tsc` clean, **302 tests**, build clean):
> - **Phase A — robust admin-reply attachment (code, locally verified).** ROOT CAUSE proven on live ticket `5aec106f`: admins only attached via a quoted reply *to a ticket root* or the hard 90-second window; an admin answering ~4 min later (the common case) fell through `disposeUnattachedMessage → drop-admin` and VANISHED. New pure module `admin-reply-attach.ts` (`selectAdminAttachTarget`, `ADMIN_UNQUOTED_ATTACH_WINDOW_MS` 30 min, env `ADMIN_REPLY_ATTACH_WINDOW_MS`) + 10 tests. Two server.ts changes: (A2) a quoted admin reply whose quoted message is a folded *follow-up* (not a root) now resolves the thread via the quoted message's **sender_hash** (reply-to metadata as ground truth) instead of dropping; (A3) the un-quoted 90s window widened to the single most-recently-active user ticket within ~30 min of last activity. `disposeUnattachedMessage` stays the final fallback so genuine admin news/banter still never becomes a ticket. **Verified e2e on the no-telegram launcher** (attach paths are pure-DB, no `tlClient`): un-quoted reply +10m attaches, +50m drops; quoted-a-follow-up attaches via sender — confirmed in the live DB, test rows cleaned up (corrections back to 69).
> - **Phase B — delete/edit handling on the WORKING path (code + schema).** The edit/delete logic was wired ONLY to the dead live `Raw` listener; the working `getChannelDifference` drain discarded `otherUpdates` (where channel edits/deletes ride). Migration `018_messages_deleted_at` (applied live, column verified) adds `messages.deleted_at`. `channel-difference.ts` gains `extractChannelEditsDeletes` (pure, +tests) + surfaces `otherUpdates`. Refactored the Raw-handler edit/delete bodies into shared `applyMessageEdit`/`applyMessageDelete` (soft-delete stamps `deleted_at`; root delete Dismisses via a guarded conditional update that can't clobber Escalated/Resolved; a deleted follow-up only soft-deletes its row) called by BOTH the Raw listener and the drain. **The live delete/edit leg over `otherUpdates` is PROD-ONLY verifiable** (no `tlClient` locally) — pure extraction unit-tested; first real exercise is the deploy.
> - **Phase C — historical backfill (data, preview-first, LIVE).** Preview EXPOSED that the support account (`7b08fcbe`, AkTayo) mixes support replies with market chatter/news/GM greetings, so time-based attribution over-attributes — the broad 30-ticket set would pollute threads (e.g. `e4336ad0` swept in 5 days of "GM Quidax Fam"/"BREAKING: Fed"). Narrowed to a hand-verified clean subset: **re-attached 50 genuine support replies to 19 single-session tickets** (sourced FROM `messages`, never hand-typed) + Dismissed the noise Open 140111. `5aec106f` now shows its full thread (2FA issue + 3 user follow-ups + all 4 admin replies). **5 multi-day/chatter-polluted tickets deliberately deferred to the Phase-2 reducer** (the only reliable attribution is reply-to metadata). Rate **38.9% → projected ~40.9%** once the enabled hourly assume-resolve sweep flips the 4 now-engaged quiet Opens (D2 may lift further). The earlier loose "~50%" was an over-estimate; the honest lever is ~+2 pts plus thread completeness + the going-forward no-drop fix.
> - **Latent risk flagged (NOT fixed here):** the reconcile sweep derives `adminSenderHashes` only from `is_admin_message=true` tickets, so a brand-new admin's dropped replies could be resurrected as bogus user tickets. Out of scope; see KNOWN_ISSUES.
> - **⏭️ DECISION PENDING (user):** commit + push (push redeploys Railway — the code fix only goes LIVE on deploy; live delete leg + channel-diff `otherUpdates` first exercised then) vs. hold. The DATA backfill is already live, so the dashboard improves without a deploy; but admin replies keep dropping until the code ships. Deploy risk mitigated by the 60s `TELEGRAM_CONNECT_DELAY_MS` + SIGTERM handler; keep `numReplicas=1`. Intern reference repo reviewed (basic Bot-API CRUD tracker — pure pitch contrast).
>
> **⚡ 2026-06-22 — ACCURACY-AUDIT INSTRUMENT BUILT + PIVOT TO BULLETPROOF INGESTION. Committed locally, NOT pushed.** See memory `session-state-2026-06-22-ingestion-pivot`.
> - **Built the credibility stack** (replaces the dismissible self-authored gold cases + selection-biased verify panel): a real-traffic agreement audit (stratified real messages → classified by the DEPLOYED `/api/eval` → judged BLIND by a human; category exact + confusion, urgency within-one + Critical/High recall, blanks excluded from the denominator) plus system-level trust metrics (auto-resolve precision, noise false-negative rate, grouping over-merge/over-split). New committed: `audit-scoring.ts` (+ tests, 285 total green), `scripts/audit-score*.ts`, `scripts/audit-sample.mjs`/`audit-system.mjs` (negated into git), `docs/ACCURACY_AUDIT_METHODOLOGY.md`, placeholder `audit-results.json`. `src/App.tsx` reframed (Real-Traffic Audit card, "Internal Regression Suite", training-loop reframe); `tsconfig` `resolveJsonModule`. Blind worksheets (`agreement_blind.xlsx` 117 rows, `system_blind.xlsx`) live in the gitignored `audit/`.
> - **The audit card is BUILD-TIME-BAKED** from `audit-results.json` (Vite constant-folds the JSON import) — `npm run build` after populating it, or it stays on "Audit pending".
> - **User feedback mid-rating:** 300 rows is too much, many sampled messages are context-free one-offs (unjudgeable even by an expert — which IS the product thesis), and the taxonomy needs a **"Community Chat"** bucket (greetings/user-to-user chatter → Spam/Irrelevant by prompt design today). Plan: confidence-gate (blanks excluded), shrink N, lead with system metrics + Live Sandbox + methodology.
> - **PIVOT (user, for the imminent pitch): make ingestion + classification bulletproof — the real selling point.** Benchmark = "work in progress". Symptoms to fix in a fresh PLAN-MODE session: dropped admin reply (Jun 20 ~21:33, suspect `disposeUnattachedMessage` drop guard), missing original message in some threads, deleted Telegram message still Open (delete handler needed), inflated Open backlog dragging the rate. Real fix = the deferred Phase 2 idempotent `messages→tickets` reducer keyed on reply-to metadata. Intern reference repo to review: https://github.com/OnuegbuUdochukwu/telegram-complaint-system
> - **STILL UNPUSHED:** this session's 3 commits + the prior `2fdc763` (verify honesty) and `a8eaa18` (Master Guide). Push redeploys Railway; held deliberately the night before the pitch. KNOWN_ISSUES/PRD not yet updated.
>
> **⚡ 2026-06-21 — BENCHMARK / VERIFICATION HONESTY + FRAGMENT CLEANUP. Pushed & live.** Triggered by the user reviewing the "AI Classifier Accuracy Benchmark" modal and seeing the Training Loop Verification panel read **0% baseline / 5% trained** with ERROR rows.
> - **Verification panel fixed (`2fdc763`).** `/api/verify` was grading the classifier against the newest 20 `corrections` rows — which are all `correction_source='admin_reply'` (categories Groq machine-inferred from an admin's reply, often on context-free fragments like a bare transaction id "2388200980"→Deposit Issue). The classifier cannot reproduce those from the bare text, so it scored ~0% by construction, and transient Groq errors (rate-limit/breaker/timeout in the 40-call burst) were counted as wrong. Fix: grade ONLY `correction_source='human_ui'` (genuine /train reviews) — the panel's own "messages a human reviewed" copy is now true; `getFewShotCorrections` (production few-shot) still draws from the full pool, untouched. Errored cases are retried once and **excluded from the accuracy denominator** (reported as amber "skipped"), so a rate-limit can never read as 0%. Verified live on the no-telegram launcher: real human pool, scored **33%→50% (+17 with training)**. **IMPORTANT: that low number is selection-biased** — the /train pool is enriched for messages a human bothered to review *because they looked wrong* (the AI's hardest cases), so it is NOT the classifier's true accuracy. Do NOT pitch the verify number.
> - **Master Guide — benchmark trust story (`a8eaa18`).** Part 5 gained a "Benchmark vs Verification — what each proves" section; Part 8 gained Q&As on how the gold benchmark is graded and why it's trustworthy (and a reframed success-metric answer); Part 9 gained a "do NOT run Verification live as the accuracy proof" note. Corrected the now-misleading "33%→100%" framing and the stale test count (172→273/21 files). `PULSEDESK_MASTER_GUIDE.md` reassembled, `.docx` re-exported + validated, root `package.json` untouched.
> - **Fragment cleanup (live DB, data-only, no commit).** Preview-first + user sign-off: Dismissed 5 context-free, never-admin-engaged fragment tickets that were dragging the active denominator — `89651cc1` "I made payment", `eb7c6730` "/p BTC" (a price command predating the noise filter), `33b83d79` "Order book also", `8c631187` "USDT/NGN", `29c1641f` "USDT too". Guarded conditional update (`.in(status, active)`), `updated_at` set explicitly. KEPT `c8478fac` "2388200980" (it had a real admin reply). Active 132→127, **resolution rate 38.0%→38.9%**. Reversible in /train.
> - **OPEN — the accuracy story is not yet pitch-credible (next session).** The user's sharp objection: the 20-case gold benchmark is self-authored, so a skeptic says "you wrote the test AND the answer key." The credibility principle = *who decides the right answer, and can they game it?* The pitch-credible plan: a **real-traffic agreement audit** judged by Quidax's own agents (their experts = ground truth) on real recent messages; grade urgency "within one level" + **recall on Critical/High** (not exact-match, since severity is subjective); add **system-level trust metrics** — auto-resolve precision (of Resolved/Assumed-Resolved, were they truly resolved?) and noise false-negative rate (did the filter ever dismiss a real issue?). Demote the gold set to an internal regression check; keep the Live Sandbox (`/api/test-message`) as the interactive "type your own message" proof; decide whether to remove/reframe the Verification panel. Full continuation prompt captured for the next session. See KNOWN_ISSUES "ACCURACY / BENCHMARK CREDIBILITY".
>
> **⚡ 2026-06-20 (night session) — PHASE 0 + PHASE 1 of the "lost / mis-classified / wrong-status tickets" rebuild. LIVE & VERIFIED IN PROD (commit `4eac458`).** This is the recurring blocker the user paused on: messages that never become tickets, one conversation split across tickets, and an untrustworthy "In Review". Plan: `.claude/plans/starry-enchanting-hammock.md` (phased; Phase 2 = full idempotent rebuild, deferred to AFTER the Monday pitch).
> - **Phase 0 — clean + honest dashboard (`e227d77`, frontend-only, no migration).** Renders `[USER_FOLLOWUP]` turns; fixed the Resolution pie (3 cells + correct legend, no mystery gray slice); display-only status relabels ("In Review"→**Admin Replied**, "Assumed Resolved"→**Likely Resolved**); derived **Handed Off** badge from a new pure `handoff-detect.ts` (DM/email redirects); "Processing message…" now shows the original text + a /train link; `min-h-[100dvh]`/`transform-gpu`/`absolute` blobs to kill the iOS scroll-shake + bottom whitespace. (Stored values/KPIs unchanged.)
> - **Phase 1a — self-healing reconciliation (`f00dfba`, NO migration).** ROOT CAUSE: the `messages` row is written BEFORE the ticket and the dedup keys on it, so any throw mid-build orphans the message FOREVER (every later re-scan skips it). New sweep `reconcileOrphanMessages()` (startup+5min, then hourly; gated `INGEST_RECONCILE_ENABLED` default OFF + `INGEST_RECONCILE_DRY_RUN` default ON; **now LIVE on Railway = true/false**) finds orphans in `messages` and replays them through the SAME `processAndIngestMessage` via a 3-line `reconcileOpts` bypass (sender_hash preserved so a multi-msg convo re-groups; existing `messages.id` reused, no dup row). Pure module `message-reconciliation.ts` (`filterReconcileCandidates` + `isSystemBotMessage` + ILIKE-probe, 19 tests). **Dropped the planned `id=` tagging** — it would have broken `conversation-grouping.ts`'s exact-match parsers (`:36`/`:41`) and the `.includes("[ADMIN_REPLY]")` admin-engaged detection (`:1148`/`:1261`); used an era-agnostic raw_text substring "already-attached" check instead. **Admin-exclusion gap caught by the dry-run:** `messages` has no senderId so `checkIsAdmin` can't run in the sweep, and dropped-admin hashes aren't in the admin-ticket set → added `isSystemBotMessage` (welcome/ban templates) + the live `shouldProcessMessage`/`isBanterNoise` gate so only REAL actionable user messages are resurrected.
> - **Phase 1b — bounded the quoted-reply fallback (`f00dfba`).** The no-time-limit sender-hash fallback (`server.ts` ~`:2497`) attached a fresh quoted reply to the sender's most-recent active ticket regardless of age (today's "Sol/USDC" → a month-old ticket). Now bounded to `last_message_at` within `QUOTED_FALLBACK_MAX_AGE_MS` (48h), most-recently-active first; past it → grouping / new ticket. (Forward-only; does not retroactively un-fragment ticket `9ef87403`/`7c36d5f4`.)
> - **Phase 1c — price-bot noise filter (`4eac458`).** `isBanterNoise` pattern **1d**: automated token price-snapshot dumps ("Price: $… USD"/"Fully Diluted Market Cap"/"View on CoinMarketCap" — two+ labels) → Dismissed lane, in BOTH the live pipeline and the sweep. A human casually mentioning a price is left untouched.
> - **Verified e2e:** `tsc` clean, **273/273 tests**, build clean. Reconcile path exercised LIVE on the no-telegram launcher (reconcile never calls `tlClient`, so it's locally testable): recovered the real 48h backlog into the prod DB — the morning account-access convo (140062/063/064) folded into **ONE** ticket `c4ba87be` (Account Access/High), `140044` correctly folded into its sibling BVN ticket, duplicate spam deduped by text-match. The 2 dropped admin replies (140065/140066) were re-attached to `c4ba87be` by SQL **sourced from the genuine `messages` rows** (status→Awaiting User, `first_admin_reply_at` stamped) — full 3-user+2-admin thread now visible. Deploy healthy: Telegram reconnected (no `AUTH_KEY_DUPLICATED`), all circuits closed; post-deploy preview shows **0 orphans remaining** (sweep is idempotent).
> - **Honest limitation (stated for the pitch):** DM/email + this one historical pair of dropped admin replies aside, reconciliation recovers the USER side from `messages` alone; it cannot reconstruct reply-to links that were never stored. Going forward the gap self-closes (the ticket exists within minutes, so later admin replies attach normally). Phase 2 (idempotent `messages→tickets` reducer using Telegram reply-to metadata as ground truth) is deferred to after Monday.
>
> **⚡ 2026-06-20 (evening session) — GQ/IN-REVIEW QUEUE CLEANUP. D2 PRECISION CONFIRMED HOLDING.**
> - **GQ queue cleanup (data-only, no code change).** Monitored the live DB after D2 went live. Both D2 resolutions confirmed correct (`065ac79e` KYC-approved, `e4336ad0` delisting+thanks). Discovered 29 "In Review / General Question / admin-engaged" tickets frozen permanently — the 7-day sweep and D2 both explicitly filter out noise categories (server.ts:1127–1130 and :1236–1240) so these would never auto-move. They were also already excluded from the rate denominator (Phase 1). One-time `UPDATE … SET status='Dismissed'` for all 29 (>7d quiet, all banter or resolved Q&A or DM hand-offs — previewed before applying). In Review 101→72; Dismissed 510→539; rate unchanged at 38.2% (expected — these tickets weren't in the denominator). **Going forward:** the same category filter in both sweeps means no new GQ/In-Review accumulation beyond a ~21 non-noise remaining. Resolution-rate watch: next D2 candidates are 13 admin-engaged non-noise In-Review tickets at 4–5d quiet (Deposit, Account Access, App Bug, Trading) — will come through next hourly sweeps.
>
> **⚡ 2026-06-20 (later session) — MANUAL-AUDIT FIXES A–D2 DONE & VERIFIED IN PRODUCTION.** The user audited the live dashboard and flagged: missing "today" messages, a nested scrollbar/iOS overflow, useless KPI tooltips, a confusing pie hover, and tickets that never resolve. Root-caused and fixed in 5 phases (plan: `.claude/plans/refactored-kindling-porcupine.md`):
> - **Phase A — quoted-reply silent drop (CRITICAL, `cc07f58`).** A genuine user issue arriving as a *quoted reply* to an old/welcome message hit `return null` in the non-admin quoted-reply branch → NO ticket created (then the admin's replies dropped as unattached-admin → the whole live conversation was invisible on the dashboard). Proven by live logs (`Quoted user reply 140062 (to 139564) matched no active ticket`). Fix: removed the `return null`, fall through to the existing grouping + new-ticket logic. Top-of-function `telegram_message_id` dedup keeps re-scans idempotent; banter still gets Dismissed. Verified e2e on the launcher (3 scenarios) + live.
> - **Phase B — feed order (`b08f106`).** `/api/tickets` ordered by `created_at desc`, so a ticket replied-to today but created days ago sank. Now `.order("last_message_at", { ascending:false, nullsFirst:false }).order("created_at", …)`. KPIs unaffected (they come from the `tickets_stats` RPC).
> - **Phase C — dashboard clarity (`b474d62`).** Removed `overflow-auto` from the dashboard root (killed the nested scroll layer + iOS horizontal overflow); added real plain-English KPI tooltips (`getKpiTooltip`/`KPI_TOOLTIPS` in `src/App.tsx` — Active Issues, In Review, Resolved, Resolution Rate, Median Response Time); fixed both pie hovers to show the slice name (Resolved/Active/category) instead of the generic "Count".
> - **Phase D1 — Assumed-Resolved ENABLED.** `ASSUMED_RESOLVE_ENABLED=true` was already set on Railway and the 7-day backfill had already run; verified live (31 Assumed Resolved, then a 32nd — `bcfda164` — crossed 7 days mid-session). NO LONGER DORMANT.
> - **Phase D2 — conversation-aware resolution inference (`21a051a` + prompt refine `ad2efda`), LIVE & VERIFIED.** New pure module `conversation-resolution.ts` (`buildResolutionMessages` + strict `parseResolutionDecision`, 12 tests) + sweep `inferResolvedFromConversation()` in `server.ts`: reads the WHOLE thread of an admin-engaged, active, non-Escalated ticket quiet ≥`RESOLUTION_INFER_QUIET_HOURS` (24h) and asks Groq (`llama-3.1-8b-instant`, temp 0, breaker+timeout+PII-redact) "did support resolve the issue?"; strict-true only, every error path leaves the ticket untouched; writes `Assumed Resolved` via the same guarded conditional update; never posts to Telegram. Two fail-safe flags mirroring the bot rails: `RESOLUTION_INFER_ENABLED` (default OFF) + `RESOLUTION_INFER_DRY_RUN` (default ON). **Dry-run preview in prod surfaced a precision miss** (closed a ticket where the admin was awaiting a screenshot) → sharpened the prompt to focus on the LAST support message + "anything pending on the user?" + 4 worked examples → re-preview clean (2 solid / 0 wrong). Went live (`DRY_RUN=false`); the live sweep correctly flipped exactly the 2 right tickets (`065ac79e` KYC-approved, `e4336ad0` delisting+thanks), stamped `resolved_at`, touched 0 Escalated. **Live result: Assumed Resolved 31→34, Active 133→130, resolution rate 37.0%→38.4%.** Railway flags left ON (`ENABLED=true`, `DRY_RUN=false`).
> - **Gotchas learned:** `railway variables --set` did NOT auto-redeploy in this CLI version — a running container keeps its boot-time env; had to `railway redeploy --yes` to make `DRY_RUN=false` take effect. `railway logs` streams only the current container's recent buffer — cross-check the live DB, which is authoritative. Enabling `RESOLUTION_INFER_ENABLED` was auto-blocked once by the permission classifier as an unauthorized prod-infra change; the user authorized it explicitly.
>
> **⚡ 2026-06-20 (earlier) — PHASES 1, 2, 3 & 4 DONE & VERIFIED IN PRODUCTION (commit `fad4fdcb`). Phase 2 sweep DORMANT pending enable.**
> **Phase 4** (noise pre-filter hardening, `fad4fdcb`, 2026-06-20): new pure module `noise-prefilter.ts` exports `isBanterNoise(text)`, OR'd into the existing `isPreFiltered` gate in `processAndIngestMessage`. Catches two patterns — **1a** bare price-bot commands (`/p BTC`, `/price zec`, `/c SOL`, `/chart xrp` — whole trimmed text only, so a question after the command still flows through) and **1b** pasted external news/promo (≥25 words, no `?`, no first-person pronoun, no Quidax/account/support keyword, starts with a proper-noun subject + reporting verb or a journalism prefix like "Breaking:"). **1c (user-to-user chatter) deliberately DEFERRED** — false-positive risk too high; existing `shouldProcessMessage` + Dismissed-on-noise-category handles it. `isBanterNoise` signals `isPreFiltered=true`: skips the LLM, assigns `category: General Question / Dismissed` (reversible in /train), never drops the `messages` row. Verified: tsc clean, **230/230 tests** (+26 in `tests/noise-prefilter.test.ts`), build clean. **Live-confirmed in prod**: commit `fad4fdcb` live at 02:09Z, all circuit breakers closed, `telegramReady:true`; no new price-command tickets expected to land in the triage queue going forward.
> **Phase 3** (conversation threading by active ticket, NO migration): the grouping branch in `processAndIngestMessage` now folds a same-sender un-quoted message across a WIDE window. Three bands keyed on the gap since the candidate active ticket's last activity (pure `groupingBand` in `conversation-grouping.ts`): `fast` (≤5 min → fold, no LLM), `extended` (5 min–6 h `GROUPING_ACTIVE_WINDOW_MS` → Groq topic-shift decides same-vs-different), `none` (>6 h → new ticket). Topic-shift = new pure module `topic-shift.ts` + `checkSameIssueViaGroq` (one `llama-3.1-8b-instant` call, breaker/timeout/PII-redacted, fail-safe to a NEW ticket on any error). The `telegram_message_id` dedup was untouched. Verified: tsc clean, **204/204** tests (+21), build clean, and all four scenarios + idempotency proven live on the no-telegram launcher via `/api/ingest` (grouping never calls `tlClient`, so the live leg WAS locally exercisable, topic-shift LLM included). **Production-confirmed 2026-06-20**: multiple live tickets have `[USER_FOLLOWUP]` blocks (e.g. `bad126a1` Withdrawal Issue, `ee682be6` Account Access), proving the fast-band fold fires on real group traffic. Extended-band topic-shift live logs not captured (railway logs stream only the current container; the previous container's history is gone) — verdict is the unit tests + the fast-band production evidence.
> **Phase 2** (migration `017_assumed_resolved`, applied live): new system-only status **`Assumed Resolved`**. A dormant hourly sweep (`assumeResolveQuietTickets`, gated by `ASSUMED_RESOLVE_ENABLED`, default OFF) moves admin-engaged tickets quiet ≥7 days into it; it counts in the resolution rate (separate `assumedResolvedCount`), and a new user message reopens it to In Review (clears `resolved_at`). Pure module `assumed-resolved.ts` + 11 tests; reopen proven e2e on the launcher. **⏭️ NEXT: enable `ASSUMED_RESOLVE_ENABLED=true` on Railway after deploy** to run the signed-off 32-ticket backfill (projected rate 22% → ~38%); confirm live (sweep's first real run is prod). Eligibility = Open/In Review/Awaiting User, NEVER Escalated.
> **Phase 1** (migration `016_kpi_honest`, applied live): `tickets_stats` returns `medianResponseMs` (`percentile_cont(0.5)`, not mean), `activeCount` excludes noise categories (General Question / Praise / Spam/Irrelevant), dead `'Classifying'` branch gone; card relabelled "Median Response Time"; 9 stuck Spam/Praise tickets Dismissed. Result: Active 240→161, response 8.4h→6.5min, rate 16%→22%.
> **Verified (both phases):** `tsc` clean, 183/183 tests, build clean; numbers reconciled vs live SQL. Phase 1 committed (`7b96d49`, `40a3f41`); Phase 2 committed this session. Phase 3 (threading by active ticket + topic-shift) and Phase 4 (noise pre-filter) remain — see the plan at the TOP of `KNOWN_ISSUES.md`.
>
> **(prior context)** The audit found the ~16% rate was faithful but the 240-ticket Active denominator was a graveyard: conversation fragmentation (one user's 4-hour chat → 14 In-Review tickets), invisible DM/email + go-quiet resolutions (101/240 active admin-engaged + quiet ≥3d), and 33% noise. The 4 locked decisions: thread-by-active-ticket + topic-shift detection; 7-day `Assumed Resolved`; exclude-never-delete noise; median response time.

## 1. What PulseDesk Is & Why It Exists
**PulseDesk** is a production-grade Telegram support triage tool built specifically for **Quidax** (a Nigerian crypto exchange). Its primary purpose is to allow their support team to immediately start using an AI-augmented triage system to manage community inquiries, drastically reducing response times and ensuring critical issues are highlighted. 
*Strategic Purpose:* This project also serves as a high-value pitch asset to demonstrate concrete, deployable value to the Quidax team.

## 2. Full Tech Stack
*   **Backend:** Node.js + Express + TypeScript (`server.ts`)
*   **Frontend:** React 19 + Vite + Tailwind CSS v4 (`src/App.tsx`)
*   **Telegram Integration:** GramJS (Persistent TCP connection for live listening)
*   **Classification Engine:** Groq API running the LLaMA model
*   **Suggested Replies:** Gemini API (specifically Gemini 3.1-Pro)
*   **Database:** Supabase (PostgreSQL)
*   **Deployment:** Containerized on Cloud Run / AI Studio (Internal Port 3000)

## 3. Environment Variables
*   `GROQ_API_KEY`: Used by the backend to authenticate with the Groq API for LLaMA-based classification of incoming messages.
*   `NODE_ENV`: Standard environment flag (`development` or `production`). Dictates strictness of security headers (e.g., Helmet CSP).
*   `ENABLE_BETA_FEATURES`: Boolean flag used for phased rollouts of experimental UI/UX features.
*   `APP_URL`: The deployed Cloud Run/AI Studio URL. Used for self-referential links or OAuth callbacks.
*   `SUPABASE_URL`: The URL endpoint for the Supabase project.
*   `SUPABASE_ANON_KEY`: The public anonymous key for Supabase (Must NEVER be used for backend operations to ensure server authority). **Dead as of 2026-06-12** — legacy JWT keys were disabled in Supabase during the service-role key rotation; nothing in production used it (only local one-off scripts).
*   `SUPABASE_SERVICE_ROLE_KEY`: The admin key used by the backend to bypass Row Level Security (RLS) and enforce server-side authority.
*   `TELEGRAM_API_ID` & `TELEGRAM_API_HASH`: Telegram developer credentials required to initialize the GramJS client.
*   `TELEGRAM_SESSION_STRING`: A generated persistent string allowing GramJS to authenticate without requiring a 2FA code on every restart.
*   `TELEGRAM_GROUP_USERNAME`: The target Telegram group to monitor (e.g., `OfficialQuidaxCommunity`).
*   `VITE_DASHBOARD_PASSWORD`: The password required by users to unlock and access the frontend React dashboard.
*   `SUPPORT_API_KEY`: An internal token/key for secure backend administrative endpoints.

## 4. Complete Data Flow
1.  **Telegram Listener:** GramJS maintains a persistent connection and listens for `NewMessage` events in the specified group.
2.  **Pre-Filter:** Checks `shouldProcessMessage` to drop spam, bot messages, or short non-actionable text.
3.  **PII Redaction:** Strips sensitive data (phone numbers, emails) before sending to external APIs.
4.  **Groq Classification:** The sanitized message is sent to LLaMA via Groq to extract JSON containing category, urgency, product area, and summary.
5.  **Gemini Reply Generation:** Gemini 3.1-Pro generates a context-aware suggested reply based on the classification.
6.  **Supabase Storage:** The backend validates the JSON (using Zod/custom fallback) and inserts the record into PostgreSQL using the Service Role Key.
7.  **React Dashboard Polling:** The frontend UI polls the `/api/tickets` endpoint every 5 seconds to display real-time updates to human agents.

## 5. Every Bug Fixed & How It Was Fixed
*   **Supabase RLS/Insertion Failures:** 
    *   *Bug:* `getSupabase()` used `SUPABASE_ANON_KEY`, causing backend inserts to fail or trigger RLS blocks. 
    *   *Fix:* Switched strictly to `SUPABASE_SERVICE_ROLE_KEY` on the backend to maintain absolute server authority and bypass RLS safely.
*   **Schema Output Validation Failures:** 
    *   *Bug:* The LLM returned custom keys (e.g., `priority` instead of `urgency`). The validator blindly defaulted unrecognized fields to `Medium` and `General Question`.
    *   *Fix:* Mapped `responseSchema` directly into the System Prompt via `JSON.stringify` and built a case-insensitive fallback mapping (`validateTicketSchema`) to guarantee DB schema compliance.
*   **Telemetry Caching Bugs:** 
    *   *Bug:* Outdated telemetry/cache data causing stale reads. 
    *   *Fix:* Corrected the caching headers and logic in the backend serving layer.
*   **PowerShell Command Execution Failures:** 
    *   *Bug:* Using `&&` in terminal commands failed due to Windows PowerShell constraints. 
    *   *Fix:* Switched to using `;` or sequential isolated commands for local scripts.
*   **Demo Mode Interference:** 
    *   *Bug:* The system was not fetching live data due to `DEMO_MODE`. 
    *   *Fix:* Disabled `DEMO_MODE=false` in the `.env` to verify live connection.
*   **Admin Reply Duplication (fixed 2026-06-11, commit `3b04b54`):**
    *   *Bug:* Admin/user replies appeared 2-3x (worst case 23x) on the dashboard. The duplicate-message check in `processAndIngestMessage` ran *after* the reply-handling branches, so AutoFetch (every 15 min, 2-hour lookback) and backfills re-appended `[ADMIN_REPLY]`/`[USER_REPLY]` blocks to tickets on every pass.
    *   *Fix:* Moved the `telegram_message_id` dedup check to the top of the function; user replies are now also recorded in `messages`. Existing data cleaned via previewed script: 446 duplicate blocks removed across 82 tickets, re-scan confirmed zero remain.
*   **Dishonest KPI Calculations (fixed 2026-06-11, commit `2ea6bea`):**
    *   *Bug:* "Resolved" metrics counted Dismissed spam as resolutions (rate showed 80%, truth 53%); "Resolved Today" read the never-maintained `updated_at` column; "today" used server (UTC) timezone.
    *   *Fix:* Dismissed excluded from all resolved metrics (Resolution Rate = Resolved ÷ (Resolved + Active)); all four status-writing paths (status endpoint, user auto-resolve, Telegram delete handler, admin-message insert) now stamp `resolved_at`/`updated_at`; "today" computed in Africa/Lagos. Verified by live API comparison and a resolve/reopen round-trip.
*   **Filter Inconsistency on KPI Cards (audited & fixed 2026-06-11, commit `9c2ea4f`):**
    *   *Audit result:* category, urgency, and date filters already reached the stats query (verified live: stats row counts tracked table totals exactly). The real problems: (a) the search box filtered the table but the KPI cards kept showing unfiltered numbers (proven: 103 table rows vs 822 stats rows); (b) a custom date range dropped the entire end day — "today to today" returned 0 tickets while 4 existed; (c) the "last N days" cutoff used the server clock (UTC on Railway) instead of Lagos.
    *   *Fix:* search moved into the shared base filters so both queries see it; all date boundaries (`days=N`, custom start/end) computed as Lagos calendar days. Verified against a server running with TZ=UTC (Railway simulation). Note: the `status` filter intentionally never reaches stats — the KPI cards break tickets down by status.
*   **Unquoted Admin Replies Created Orphan Tickets (fixed 2026-06-11, commit `6c66bf2`):**
    *   *Bug:* When an admin answered in the group without quoting the user, the system could not link the reply to a ticket, so it was stored as its own separate Resolved "ticket" and the real ticket looked untouched.
    *   *Fix:* 90-second window heuristic in `processAndIngestMessage` — an unquoted admin message attaches to the most recently ingested Open/In Review non-admin ticket in the same group if that ticket arrived within the previous 90 seconds; the ticket gets the `[ADMIN_REPLY]` block and is marked "In Review". Outside the window, behavior is unchanged. The top-of-function dedup makes re-scans idempotent. Verified end-to-end via the extended `/api/ingest` endpoint (attach-within-window, ignore-outside-window, re-ingest idempotency), test data cleaned up.

*   **Quoted Admin Reply With a Missing Parent Lost the Thread (fixed 2026-06-13, Fix 5, commit `e848d5f`):**
    *   *Bug:* When an admin replied in the group by quoting a user's message whose original was never ingested (the live listener was down and the message predated AutoFetch's 2-hour window), there was no ticket to attach to — the reply became its own standalone, already-Resolved admin "ticket" and the real conversation thread was lost (audit 2026-06-12, KNOWN_ISSUES §6 item 8).
    *   *Fix:* In `processAndIngestMessage`'s quoted-admin branch, when the quoted parent has no ticket in our DB, the system now fetches that message from Telegram by id (`client.getMessages(targetGroup, { ids: [id] })`), ingests it as a root ticket, then attaches the admin reply (Open→In Review, `[ADMIN_REPLY]` block, `first_admin_reply_at` stamped). The orchestration is a pure module (`quoted-parent.ts` → `recoverQuotedParent`, 5 unit tests) with the Telegram fetch injected, so it is unit-testable without a live session and is a safe no-op on the no-telegram launcher (behavior then unchanged). It composes with Fix 1 (the reply's own `messages` row is already persisted; the parent carries a different telegram id, so nothing double-inserts) and ingests the parent as a ROOT message (no `replyToMsgId`) so recovery can never recurse up a reply chain. Proven by the unit tests plus a live-DB check of the attach-after-resolve path; all test data cleaned up and `corrections` returned to baseline (2). The real Telegram fetch path cannot be exercised on the local launcher and ships unverified against a live missing-parent until production observation.

*   **Live Telegram Listener Was Dead & the Watchdog Was Blind (fixed 2026-06-13, Fix 6, commit `1a7130d`):**
    *   *Bug:* the live `NewMessage` listener had effectively stopped ingesting group messages for days (only the 15-min AutoFetch sweep was working — see KNOWN_ISSUES §6 item 1). Two causes: (a) the handler stamped the watchdog's "last message seen" clock BEFORE checking the message was even in our group, so private messages and unrelated groups kept the clock warm and the 30-minute reconnect watchdog never fired on a silently-dead group connection (corroborated live: production `/api/health` showed a "now" timestamp while the group listener was dead); (b) "is this our group?" was a loose username/title/"quidax" substring guess instead of an exact match on the resolved channel id.
    *   *Fix:* the watchdog clock is now stamped only when a message is actually in the target group; group membership is decided by the exact resolved channel id (`resolveTargetChannelId`), falling back to the old name/title heuristic only during the brief startup window before that id resolves, so nothing is dropped meanwhile. The decision logic lives in a pure module `listener-health.ts` (`isMessageInTargetGroup`, `matchPath`, `shouldReconnect`) with 16 unit tests, mirroring `telegram-guards.ts` — necessary because the live listener path is null on the no-telegram launcher and can't be exercised locally. The live message log now records `chatId` and whether the match was `channelId` or `fallback`, so a short production observation can confirm real group messages are flowing and that `lastMessageReceivedAt` now only advances on group traffic. The live leg's first real exercise is production (stated honestly, same as Fix 5).

*   **AI Benchmark Was Dead in Production (fixed 2026-06-13, Fix 7, commit `c8b2ea9`):**
    *   *Bug:* the "AI accuracy benchmark" modal showed nothing useful in production — a blank "%" score, a "Needs tuning" badge, an empty results table, and a misleading "Running 20 test messages" loading line. Root cause: the 12 gold test cases lived in `benchmark_cases.json`, which is gitignored (a one-off-artifact convention) and therefore never deployed to Railway. `/api/eval` logged "Could not load benchmark_cases.json", fell back to an empty array, and returned `total: 0` with NaN→null accuracies (KNOWN_ISSUES §6 item 6). The eval loop also fired Groq calls back-to-back with no spacing.
    *   *Fix:* the 12 cases now live in a committed TypeScript module (`benchmark-cases.ts`) that bundles straight into `dist/server.mjs`, so they always ship (mirrors the `telegram-guards.ts` / `listener-health.ts` pure-module convention). `/api/eval` imports the module (POST `{messages:[…]}` still overrides for the file-upload path), spaces Groq calls 2.1s apart like the verify loop, and returns `null` (not NaN) accuracies when there are no cases. The modal drops the fake count from its loading text and renders a neutral "No benchmark cases available" empty state instead of a bare "%". The `eval.ts` CLI reads the same module so the API and CLI can never drift. A structural test (`tests/benchmark-cases.test.ts`, 6 cases) guards that exactly 12 cases ship with valid categories/urgencies. Verified live on the `c8b2ea9` deploy: production `/api/eval` returned `total: 12` with real numbers (category 100% / urgency 75% / overall 75%) and a 12-row table. `/api/eval` stays a raw-model baseline (NO few-shot injection — deliberate). Known gap left untouched: the 12 cases are Quidax-representative English with no Nigerian Pidgin.

*   **Nigerian Pidgin English Misclassified (fixed 2026-06-13, Fix 9, commit `0354351`):**
    *   *Bug:* Common Pidgin support phrases — "e don do", "dem block my account", "money never enter", "abeg help me", "una too much" — were being misclassified as `General Question` or given the wrong urgency (e.g. "my money dey inside" = money present in locked account was read as Critical instead of High; "Quidax na correct exchange! withdrawal enter sharp sharp, una too much" was read as General Question instead of Praise). KNOWN_ISSUES §3, referenced in Fix 7's known gap.
    *   *Fix:* New pure module `pidgin-glossary.ts` exports `PIDGIN_GLOSSARY_PROMPT` — a phrase-to-meaning-to-category mapping + 5 worked examples (different wording from the benchmark). `server.ts` appends it to `GROQ_SYSTEM_PROMPT` so Pidgin context reaches the main pipeline, Gemini fallback, and `/api/test-message`. Critically it also flows to `/api/eval` (which is the raw-model baseline — the glossary is base prompt, not few-shot, so the benchmark stays comparable). `benchmark-cases.ts` extended from 12 to 18 cases: 6 new Pidgin cases (ids 13–18) covering withdrawal hang, deposit not credited, account blocked, KYC pending, app crash, and Pidgin praise. `tests/benchmark-cases.test.ts` updated (length guard 12→18, Pidgin-presence guard). `tests/pidgin-glossary.test.ts` (7 new unit tests: phrase coverage, prompt completeness, valid labels, generalisation guard). Proven: 6 Pidgin cases 83%/83%/67% before → 100%/100%/100% after; full 18-case benchmark 100% category / 94% urgency / 94% overall (the one urgency miss on English case 9 is pre-existing). 71/71 tests pass. **LIVE-VERIFIED in production (2026-06-13, `22d71ba` deploy, the push that carried this fix):** `GET /api/eval` returned `total:18`, category 100% / urgency 94% / overall 94%, with all 6 Pidgin cases (ids 13–18) correct on BOTH category and urgency (6/6 / 6/6). The sole miss is the pre-existing English case-9 urgency. Because the Pidgin glossary is part of the base `GROQ_SYSTEM_PROMPT` (not few-shot), `/api/eval`'s raw-model baseline exercises the exact prompt the live pipeline uses — so this is a true production confirmation, with no local-only caveat (unlike the live-log items still open for Fix 6/8).

*   **Gemini Suggested Replies Were Re-Burning an Exhausted Quota (fixed 2026-06-13, Fix 8, commit `7a09b15`):**
    *   *Bug:* suggested replies failed across the board from ~21:34Z Jun 12 (KNOWN_ISSUES §6 item 7). Every 15-min repair sweep, attempt 1 failed instantly and the shared `gemini` circuit breaker opened. Root cause: `isRetryableLLMError` treats a 429/"resource exhausted" as retryable-with-short-backoff, so a daily free-tier quota error made `generateSuggestedReply` fire **3 real Gemini calls per ticket** — tripping the breaker (threshold 3) every cycle and re-hammering the already-exhausted quota. The breaker recovers after 30s, so the next sweep re-burned it again. The retry log line carried only `e.message`, so the failure mode could only be guessed.
    *   *Fix:* (a) **log the real detail** — failure logs now emit `describeLLMError(e)` (HTTP status, statusText, errorDetails) so the failure mode is confirmed from Railway logs; (b) **quota-aware cooldown** — when `isQuotaExhaustedError(e)` matches a genuine quota / 429 / RESOURCE_EXHAUSTED error (deliberately NOT a transient 503/overload, which still uses the existing short-backoff retry), `generateSuggestedReply` arms a 60-minute cooldown and returns immediately after 1 failing call instead of retrying. During the cooldown the repair sweep skips with a single log line and the live pipeline never calls Gemini, so the quota is no longer re-burned and the breaker stays closed for the Gemini classification-fallback path. Quota detection + error description live in the pure module `gemini-quota.ts` (mirrors `classification-policy.ts` etc.), 9 unit tests. Scoped to `generateSuggestedReply` only (the rare Gemini classification-fallback call site is out of scope — smallest change). Proven: tsc clean, 62/62 tests, build OK, new logic confirmed bundled into `dist/server.mjs`. **The real-log confirmation of the ACTUAL error detail is a post-deploy step** (Railway logs after the next sweep) — same honesty caveat as Fixes 5/6. The cooldown only engages on a true quota error, so if production logs show instead a 404 from the `gemini-3.5-flash` reply model name (note: the classification fallback uses `gemini-2.5-pro`), behavior is unchanged and a model change becomes the follow-up.

*   **Live Listener Received No Group Messages — the Session Was Never Primed (fixed 2026-06-13, Fix 10, commit `14abd74`):**
    *   *Bug:* even after Fix 6 (the watchdog), the live `NewMessage` listener still ingested NOTHING from the group — the 15-min AutoFetch sweep was doing 100% of ingestion. Fresh live-DB proof on the `22d71ba`/Fix-6 build: the minimum ingest lag across the 25 newest messages was **185.8s** (live delivery would be 1–5s) and the `ingested_at` values clustered into 15-min sweep batches; no `Live message received` line ever appeared. The SAME client's `getEntity`/`getMessages` (AutoFetch) worked every sweep, so the session/auth/connectivity were healthy — the failure was specific to the update PUSH stream. Root cause: GramJS 2.26.x has a push-only update system — `catchUp()` is a no-op stub and there is no `getDifference`/`getChannelDifference` recovery (`node_modules/telegram/client/updates.js`) — so to be pushed `UpdateNewChannelMessage` for a supergroup the session must be primed once with `client.getDialogs()` after connecting from a `StringSession`. The listener never primed. Corroborated by the audit's own logs: the Raw edit/delete handler historically received updates from OTHER chats (ids 221507/55659) while the target group's new-message updates were absent — the push stream was alive but did not include our group. (Also reconciled the misleading `/api/health` clock: `lastMessageReceivedAt` is initialised to BOOT time and the watchdog never resets it, so a freshly-deployed-but-dead listener looks "healthy" for up to 30 min — `20:05:57Z` was the deploy time, not a live message, with the last real DB ingest at `17:28Z`.)
    *   *Fix:* a new guarded `primeChannelUpdates(reason)` calls `client.getDialogs({ limit: 100 })` at startup (AFTER the handlers are registered, so there is no first-update gap) and after every watchdog reconnect (a reconnect re-establishes the session and must re-prime). The membership decision is the pure module `dialog-priming.ts` (`findTargetInDialogs`, 9 unit tests) which REUSES `isMessageInTargetGroup`/`matchPath` from `listener-health.ts` so it can never drift from the live in-target check. The call is fail-safe (never throws) and a safe no-op on the no-telegram launcher (`tlClient` null). It logs whether the target group is PRESENT in the dialog list — which doubles as the definitive membership probe the 2026-06-12 audit could not run without a second live session: if ABSENT, the account is not a member (the Milestone 5 `USER_BANNED_IN_CHANNEL` ban), no code change can revive live delivery, and the fix is account-level (swap the Telegram account / session string). Verified: tsc clean, 80/80 tests (9 new), build OK + symbols bundled into `dist/server.mjs`, and a local no-telegram boot on PORT=3100 served `/api/health` ok with the listener correctly skipped (no errors, no second GramJS session). **The live leg's first real exercise is production** (same honesty caveat as Fixes 5/6 — `tlClient` is null locally). On the next deploy, the logs decide it: either (a) `Primed channel updates (startup) - target group present in dialogs` + the first-ever `Live message received`/`matchedBy:"channelId"` line + sub-5s DB ingest lag → Fix 6/10 CLOSED; or (b) `TARGET GROUP IS ABSENT` → the account ban is the real root cause and a Telegram-account swap is required.
    *   **VERDICT (2026-06-14, prod deploy `a1c3958`, read via `railway logs`): outcome (a) on membership, but live delivery STILL FAILS — Fix 10's hypothesis is DISPROVEN.** Every startup + reconnect logged `Primed channel updates … target group present in dialogs` with an exact channel-id match (`matchedBy:"channelId"`, id 2129818880) → the account IS a member, so no account swap is needed (the Milestone-5 ban does not extend to reading). BUT the live listener delivered NOTHING: zero `Live message received` lines in 12h, DB ingest lag min 193.8s / newest 404.4s (all AutoFetch-batched), `lastMessageReceivedAt` frozen at boot. So `getDialogs()` priming is necessary-looking but NOT sufficient — GramJS 2.26.x does not push this supergroup's updates even to a primed, member session. The live-push root cause is deeper and is now PARKED as a research spike (see Fix 11).

*   **Watchdog Reconnect-Loop + AutoFetch Made the Primary Path (2026-06-14, Fix 11, `autofetch-dedup.ts`):**
    *   *Bug:* (a) reading the `a1c3958` logs surfaced a runaway loop — the 5-min watchdog (30-min silence threshold) was force-disconnecting, reconnecting, and re-priming the GramJS socket EVERY 5 MINUTES indefinitely (logs: `No Telegram messages received for 500…530 minutes` and climbing), because only live target-group messages advance `lastMessageReceivedAt` (Fix 6), none ever arrive (the live listener is dead per the Fix 10 verdict), and the reconnect path never reset that clock. (b) With live push unwinnable, AutoFetch's 15-min interval was the floor on ingest lag (~180–400s), and each sweep ran `checkIsAdmin` (a Telegram round-trip) + a 2.1s Groq-spacing sleep on all 20 messages every pass, mostly already-ingested duplicates — making a shorter interval wasteful as-coded.
    *   *Fix:* (a) stamp `lastMessageReceivedAt = Date.now()` after a successful reconnect so the watchdog backs off to its intended ~30-min cadence (a genuinely dead socket still trips again in 30 min; `connectionRetries` covers real drops between). (b) AutoFetch interval cut 15min→3min (lag target ~60–180s, and a smaller per-sweep window is less likely to overflow the `limit:20` cap), with a new pure module `autofetch-dedup.ts` (`sweepCandidateIds` / `selectMessagesToIngest`, 15 unit tests) doing ONE batched `messages.telegram_message_id` IN-query per sweep to skip already-ingested ids BEFORE the per-message `checkIsAdmin` + sleep. The authoritative idempotent dedup still lives at the top of `processAndIngestMessage`; the pre-dedup is fail-open (a lookup error processes the full window and the in-function dedup still prevents double-ingest). The live `NewMessage` listener code is left intact (harmless; may work if the deeper GramJS bug is ever fixed). tsc clean, 95/95 tests (15 new), build OK + symbols bundled into `dist/server.mjs`. **Live behavior ships unverified locally** (`tlClient` null on the no-telegram launcher) — post-deploy proof is that the 5-min reconnect/re-prime spam stops and DB ingest lag drops from ~180–400s toward ~60–180s. **Deferred (not in this fix):** the real GramJS live-push investigation (`client.start()` vs `connect()`, update-loop re-arming) — low confidence, unverifiable locally, a separate research spike.
    *   **VERIFIED IN PRODUCTION (2026-06-14 ~08:53Z, deploy `38f9d57`, evidence via `railway logs` + a live DB lag query on `dovgochitqpuvmneqeqz`):** all three checks held. (1) **Cadence** — 20 consecutive `Periodic check` sweeps exactly ~3 min apart (07:43:24→08:40:24Z), was 15 min. (2) **Watchdog loop gone** — over the 58-min window since the 07:43:23Z boot, `Force reconnecting` fired EXACTLY ONCE, at 08:13:24Z (boot + 30 min), with the silence counter reading `No Telegram messages received for 30 minutes` (not the old climbing 500→530), a clean `Reconnected to Telegram` + `Primed channel updates (reconnect) … target group present in dialogs` (channelId 2129818880), and `/api/health.lastMessageReceivedAt` advancing off its frozen boot value 07:43:23Z → 08:13:25Z (the reconnect now resets the clock — the core Fix 11 change; the old bug would have produced ~6 reconnects by then). (3) **Ingest lag dropped** — the first post-deploy group messages (telegram ids 139647–139650, ingested 08:49–08:52Z) measured 37.2 / 38.2 / 40.6 / 173.9s, squarely in the ~60–180s target (three under 60s) vs ~193–765s before. **STILL OPEN (parked research spike, NOT addressed by Fix 11):** the live `NewMessage` push listener still delivers nothing — `lastMessageReceivedAt` only ever advances on a watchdog reconnect, never on a real group push — so AutoFetch remains the sole working ingestion path. Pursue the deeper GramJS push-stream fix (`client.start()` vs `connect()` / update-loop re-arming) only as its own task; it ships straight to prod (unverifiable locally).

*   **Live Listener Root Cause IDENTIFIED — GramJS Never Syncs Channel `pts` (Research Spike, 2026-06-14, diagnostic in commit `1f110a5`):**
    *   *The open question (left by Fix 10/11):* the live `NewMessage` listener delivered NOTHING from OfficialQuidaxCommunity even though the account is a member and `getDialogs()` priming succeeds on every startup/reconnect (exact channelId 2129818880). Fix 11 made AutoFetch (3-min) the primary path and parked the live-push cause as a research spike.
    *   *Method:* shipped an opt-in diagnostic — `LISTENER_DEBUG` flag (default OFF, changes nothing unless set) + `describeUpdate` in `telegram-guards.ts` (6 new unit tests) + a guarded Raw handler in `server.ts` that logs the `className`/`channelId`/`pts`/`ptsCount` of EVERY update the session receives (METADATA ONLY — never message text, so no PII). Armed it on prod via the Railway CLI (`railway variables --set "LISTENER_DEBUG=true"`), captured the raw update stream with `railway logs`, correlated against the live DB ingest-lag query on `dovgochitqpuvmneqeqz`, then disarmed.
    *   *Verdict (PROVEN with live evidence, not hypothesised):* GramJS 2.26.x keeps NO per-channel `pts` state and never calls `updates.getChannelDifference` (`catchUp()` is a no-op; `getDifference`/`getChannelDifference` exist only in the TL type defs and are never invoked — confirmed by grep of `node_modules/telegram`). Because the session is "out of sync" on the supergroup's message sequence, Telegram WITHHOLDS `UpdateNewChannelMessage` for channel 2129818880 while still pushing (a) the channel's lightweight CONTROL updates — captured `UpdateReadChannelInbox channelId:"2129818880" pts:215292` — and (b) the account's entire common/DM update box (`UpdateNewMessage`/`UpdateShortMessage`, user-pts 455453→455464). Two distinct pts sequences: channel ~215,292 vs user ~455,464. Hard proof: test messages 139652/139653 posted 13:38:43Z produced ZERO `UpdateNewChannelMessage` in the live stream and reached the DB only via the 13:41:34 AutoFetch sweep (~174s lag, classified Praise + General Question).
    *   *Disproven (do NOT re-attempt):* the ChannelTooLong hypothesis (none EVER seen — the fix is NOT "trigger on ChannelTooLong"); the ban/membership branch (channel CONTROL updates for 2129818880 DO arrive, and priming finds the group); `connect()` vs `start()` (both wire `updateCallback: _handleUpdate` and start `_updateLoop` identically — `TelegramClient.js:1070/1090/1103`). Fix 10's `getDialogs()` priming is necessary-looking but insufficient because it lists the dialog without syncing channel `pts`.
    *   *The fix this points to (Phase 2 — separate task, ships to prod, needs explicit go):* a `getChannelDifference`-based live ingestion path — seed + track the channel `pts`, call `updates.GetChannelDifference` on a short interval (and/or triggered by any channelId-2129818880 control update), feed `new_messages` into `processAndIngestMessage` (the idempotent top-of-function dedup makes AutoFetch overlap safe), and advance the tracked `pts`. Additive to AutoFetch (which stays the safety net). Target: sub-5s ingest lag + a real live-channel-message line — the spike's original success criteria, now reachable. The `LISTENER_DEBUG` diagnostic was left in the code (default OFF) for future re-use.

*   **Phase 2 — `getChannelDifference` Live-Ingestion Path BUILT (2026-06-14, behind `CHANNEL_DIFF_ENABLED`, default OFF, `channel-difference.ts`):**
    *   *What & why:* the proven cure for the dead live listener (root cause above). Instead of waiting for a push that never comes, the server actively syncs the channel `pts` and polls `updates.GetChannelDifference` every 15s, feeding new messages into the SAME idempotent `processAndIngestMessage`. Additive to AutoFetch — AutoFetch (3-min) stays the safety net, untouched. Target: sub-15s ingest lag (vs AutoFetch's ~37–174s).
    *   *Design (mirrors the pure-module convention):* a new pure module `channel-difference.ts` owns all the risky shape/normalization logic — `classifyChannelDifference` (discriminates `updates.ChannelDifference` / `ChannelDifferenceEmpty` / `ChannelDifferenceTooLong` by `className`, reads the new pts from `resp.pts` or, for TooLong, `resp.dialog.pts`), `normalizeDiffMessage` (raw TL `Api.Message` → the exact `processAndIngestMessage` args: text from `.message`, sender from `fromId.userId`/`channelId`, username joined from the response `users[]`; returns null for service/empty/media-only/no-id messages), `buildUsernameMap`, and `sortDiffMessagesOldestFirst`. **`ChannelDifferenceTooLong` carries NO messages and is NEVER bulk-ingested** — its `messages` are latest-state, not the gap, so we only re-seed the pts and let AutoFetch's 2h lookback carry the gap (same stance as a watchdog reconnect re-seed). 23 unit tests (`tests/channel-difference.test.ts`), every branch + null/garbage inputs.
    *   *Wiring in `server.ts` (all inside the existing connected closure):* `CHANNEL_DIFF_ENABLED` flag (default OFF); `trackedChannelPts` + a re-entrancy guard; `seedChannelPts` (via `Api.channels.GetFullChannel` → `fullChat.pts`, fail-safe — retries next poll on error); `pollChannelDifference` (bounded 10-iteration drain, 2.1s Groq spacing per message like AutoFetch, fail-open). On real delivery it stamps `lastMessageReceivedAt` (keeps the watchdog asleep, like the NewMessage handler) and logs the success line `Live channel message via getChannelDifference`. Started only when enabled (15s `setInterval`, after the startup prime); re-seeded after every watchdog reconnect. Idempotency is unchanged — the authoritative dedup stays at the top of `processAndIngestMessage`, so AutoFetch overlap is safe.
    *   *Verified locally:* `tsc --noEmit` clean; 124/124 tests (23 new); `npm run build` OK + symbols bundled into `dist/server.mjs`; no-telegram boot on PORT=3100 served `/api/health` ok with the listener (and the diff path) correctly skipped (`tlClient` null → no second session, no errors).
    *   **VERIFIED IN PRODUCTION (2026-06-14, deploy `d9b11b3c`, via `railway logs` + live DB on `dovgochitqpuvmneqeqz`):** Set `CHANNEL_DIFF_ENABLED=true` on Railway → redeploy → `CHANNEL_DIFF_ENABLED - starting getChannelDifference poll (15s) alongside AutoFetch` logged at startup + `Seeded channel pts (startup)` (pts:215301, targetGroup:OfficialQuidaxCommunity). Posted test message "Alright" (telegram_id 139661, message_timestamp 17:28:35Z) → `Live channel message via getChannelDifference` at 17:28:51Z (`count:1, newlyIngested:1, newPts:215302`). DB ingest lag: **14 seconds** (ingested_at 17:28:48.642Z) vs AutoFetch baseline of 60–177s — sub-15s target met. Dedup check: exactly **1 row** in `messages` for telegram_id 139661 (no double-ingest with AutoFetch). Watchdog: no `Force reconnecting` in 16-min window since boot; AutoFetch running every 3 min as the safety net. `CHANNEL_DIFF_ENABLED=true` remains set on Railway. **KNOWN_ISSUES §6 item 1 FULLY CLOSED.**

*   **Admin Messages Became Standalone Resolved Tickets (fixed 2026-06-14, Bug 3, commit `cda3ebc`):**
    *   *Bug:* a message from a known admin that did not attach to a user ticket (no quoted parent recovered, no 90s-window match) fell through to the single ticket insert and was created as a Resolved ticket — 169 such tickets accumulated in production, polluting the dashboard and inflating the Resolution Rate KPI.
    *   *Audit:* the suspected `checkIsAdmin`/`getChannelDifference` senderId-format bug was a RED HERRING — `checkIsAdmin` runs on all four ingestion paths and the diff path's `extractSenderId(fromId.userId)` yields the same decimal string; the "Resolved" status itself proved admin detection was succeeding. The real cause was the shared downstream disposition in `processAndIngestMessage`.
    *   *Fix:* new pure module `admin-message-policy.ts` (`disposeUnattachedMessage`, 4 unit tests) + one guard before the ticket insert: an unattached admin message is dropped (its `messages` row is still persisted, so dedup holds), never ticketed. One guard fixes live/AutoFetch/getChannelDifference/backfill/`/api/ingest` at once. Proven e2e on the no-telegram launcher. Data cleanup: 170 historical admin tickets deleted (`scripts/cleanup-admin-tickets.mjs`, previewed + approved; 1 with a real `[USER_REPLY]` excluded; `messages` rows kept).
*   **Resolution Detection Too Narrow (fixed 2026-06-14, Bug 4, commit `510848e`):**
    *   *Bug:* confirmed live exchange — user "can I send money from Quidax wallet to another wallet?" + admin "Yes, you can send crypto…" — left the ticket In Review / Trading Problem instead of Resolved / General Question.
    *   *Fix:* (a) `RECLASSIFY_SYSTEM_PROMPT` now also returns `{resolved}`; an affirmative/direct admin answer auto-resolves the ticket via a GUARDED write (Open/In Review only, conditional `.in("status",…)`, never un-parks Escalated/Awaiting User; `resolved_at` stamped). Logic in pure module `admin-reply-resolution.ts` (11 unit tests). (b) `GROQ_SYSTEM_PROMPT` gained a feature-existence rule ("can I…?"/"is it possible to…?" with no problem → General Question) + benchmark cases 19-20 (base-prompt, not few-shot). The "never status" invariant in CLAUDE.md was deliberately revised — see the Bug 4 lesson there.
    *   *Evidence:* the exact confirmed example, replayed e2e on the launcher, ended **Resolved / General Question** (live DB verified). The category half also required deleting a stale POISON few-shot correction (`1ff50568`, old reclassify, message → Trading Problem) plus 2 sibling poison rows — the KNOWN_ISSUES §5 "wrong admin-reply correction propagates" limitation, surfaced and cleaned. 138 unit tests green; full 20-case `/api/eval` regression to confirm in production after deploy.

*   **Mobile Responsive Layout (fixed 2026-06-15, Bug 2):** the dashboard had no mobile design — at ~375px the header bar and the simulator/backfill control row were non-wrapping flex rows that overflowed the viewport (forcing a sideways scroll, making content look off-centre); the KPI grid/charts already stacked but padding/gaps didn't scale. **Fix (className-only, `src/App.tsx`):** header stacks + controls wrap on mobile; simulator control row stacks/wraps; `<main>`/KPI grid scale padding+gap (`px-4 sm:px-6`, `gap-4 sm:gap-6`, `p-5 sm:p-6`); search full-width on mobile; ticket-feed header + pagination wrap. Desktop (≥md) unchanged — all restores gated behind `sm:`/`md:`, and `flex-wrap` is a no-op when content fits one line. **Verified by DOM geometry at 375/768/1440px** (the preview screenshot tool timed out in this env — heavy backdrop-blur + Recharts — so verification used `getBoundingClientRect` + the a11y snapshot): zero horizontal page overflow at all widths; mobile stacks/wraps; 1440 is identical to before. `tsc` clean, 138/138 tests. **Lesson:** to verify responsive layout in this repo, prefer geometry/snapshot over `preview_screenshot` (it stalls on this page).

*   **Dashboard Rate-Limited Itself (fixed 2026-06-15, commit `768ba95`):** the global `/api/` limiter (200 req/15min/IP, `server.ts`) was lower than the dashboard's own 5s polling (~180/15min per open tab), so a tab tripped the cap in ~17 min and then EVERY request (tickets, communities) 429'd → blank KPIs, "Loading communities…", and "Too many requests from this IP" in the benchmark/verify modals. **Fix (`server.ts` + `src/App.tsx`):** global cap 200→1200, `heavyLimiter` (eval/verify/backfill) 5→20, dashboard poll 5s→10s. **Verified locally:** a 250-request unauthenticated burst to `/api/tickets` all passed the limiter (0×429; the old 200 cap would have blocked ~50 — the limiter runs before auth so each passes as a fast 401); `tsc` clean; 138/138 tests. **Data was never lost** — a direct DB query during the incident showed 701 tickets / 924 messages intact; the empty dashboard was purely the 429 cascade. Needs the Railway redeploy to take effect in production. See KNOWN_ISSUES §9 for the full 2026-06-15 triage.

*   **Blank White Page on iOS Safari (Bug 1, fixed & verified 2026-06-19, commit `1e90ff4`):** the dashboard/login showed a blank white page in iPhone Safari but worked in the Telegram and Google in-app browsers. **Key insight: all iOS browsers use the same WebKit engine, so it could NOT be a JS/CSS-compatibility bug** (the original handoff guess) — those would fail everywhere equally. Diagnosed live on the user's iPhone 14 Pro / iOS 26.5 with a Web Inspector Safari extension: Network showed all three assets `200` (nothing failed to load), the React login screen WAS in the DOM, and the page only painted after a repaint trigger (rotate / tap / opening the inspector). That repaint-to-appear signature is a WebKit **compositing** failure. Root cause: oversized decorative blur "glow" blobs (`blur-[120px]` + `blur-[150px]`) plus a `backdrop-blur-xl` card on the full-screen `min-h-screen` containers; iOS Safari intermittently fails to composite those large blur layers on first paint → white until a forced redraw. **Fix (className-only, `src/App.tsx`, all 3 such views — login, /train, dashboard):** reduced the blob radii (`120px`→`80px`, `150px`→`90px`) and added `transform-gpu` (`translateZ(0)`) to the blurred elements + their containers so Safari GPU-composites them up front. No visual change on desktop/Android. **Verified:** tsc clean; desktop preview renders the login identically with zero console errors; production CSS confirmed (120/150px blurs gone, `translateZ(0)` present); and the **user confirmed it now loads cleanly on iOS 26.5 Safari across multiple reloads**. The iOS-specific paint behaviour cannot be reproduced on desktop or this repo's preview-screenshot tool (it stalls on this blur-heavy page), so the user's device was the authoritative verification.

*   **Phase 4 — Noise Pre-Filter Hardening (2026-06-20, commit `fad4fdcb`):**
    *   *Problem:* Bare price-bot commands (`/p BTC`, `/price zec`) and long pasted news articles were clearing the `shouldProcessMessage` gate, burning a Groq call, and landing as Open "General Question" tickets the support team had to dismiss by hand. Examples visible in the live DB: tickets `9209bc1e` (`/p BTC`), `55bf2345` (`/p sol`), `bd411bba` (`/p qdx`), `b2a001fc` (Bleep social-network promo paste).
    *   *Fix:* new pure module `noise-prefilter.ts` exports `isBanterNoise(text)`. Pattern 1a: the entire trimmed message must match `^\s*\/(?:p|price|c|chart)\s+[a-z0-9]{2,10}\s*$` — a question or sentence after the command is NOT caught (intentional). Pattern 1b: AND-gate of five conditions: ≥25 words, no `?`, no first-person pronoun, no Quidax/account/support keyword, AND starts with a proper-noun + reporting-verb phrase or a journalism keyword (`Breaking:`, `Report:`, etc.). Both patterns err heavily toward NOT catching (false-negative is safe; a false-positive hides a real issue). `isBanterNoise(text)` is OR'd into the existing `isPreFiltered` check: `isPreFiltered = !skipPreFilter && (!shouldProcessMessage(text, ...) || isBanterNoise(text))`. When `isPreFiltered=true`: LLM skipped, ticket inserted as `category:"General Question" / status:"Dismissed"` (reversible in /train), the `messages` row always persisted (dedup unchanged). Pattern 1c (user-to-user chatter) deliberately deferred — false-positive risk too high at current volume. Verified: tsc clean, **230/230** tests (+26, `tests/noise-prefilter.test.ts`), build clean. **LIVE: commit `fad4fdcb` deployed 2026-06-20T02:09Z, all circuit breakers closed, `telegramReady:true`.**

*   **Milestone 5 bot-send re-tested on session #3 — still blocked (2026-06-19):** the `USER_BANNED_IN_CHANNEL` block was suspected to be specific to the old session-#1 account, so the outbound bot was re-tested end-to-end on session #3. Phase A (dry-run on Railway with `BOT_REPLIES_ENABLED=true`) produced a clean `dry_run` `bot_replies` row — the whole eligibility/dedup/rate-limit pipeline works on session #3. Phase B (one controlled live send with `BOT_REPLIES_DRY_RUN=false`) failed identically with `400: USER_BANNED_IN_CHANNEL`. **Conclusion: not an account ban — OfficialQuidaxCommunity is broadcast-only / posting is admin-restricted** (the user cannot manually post either). No code fix possible; the feature needs the session account granted posting rights (a Quidax-side decision — the pitch is the natural moment). Test cleanup: deleted the new failed row (`190ea97a…`), reopened the test ticket, reset flags to `BOT_REPLIES_ENABLED=false` + `BOT_REPLIES_DRY_RUN=true`. See KNOWN_ISSUES §7.

## 6. Every Feature Added & Why
*   **Async Ingestion Pipeline:** To prevent the GramJS listener from blocking the main thread during high message volume.
*   **Min Urgency Filter (Extraction Filtering):** Added a `minUrgency` selector to the Backfill modal and `/api/backfill` to allow historical extraction of only `High` or `Critical` tickets, preventing DB bloat with general chatter.
*   **Gemini 3.1-Pro Integration:** Added for high-quality, empathetic suggested replies, reducing the cognitive load on agents.
*   **Amber Classification Indicators:** Added to the UI to quickly visually signal items needing human review.
*   **Editable Suggested Reply Fields:** To allow human agents to tweak AI responses before sending them, enforcing the "Human in the Loop" philosophy.
*   **Conversation Grouping (2026-06-15 subtasks 1–6, 2026-06-19 subtask 7 — ALL 7 COMPLETE):** folds a user's consecutive un-quoted messages within a rolling window (`GROUPING_WINDOW_MS`, default 5 min, env-overridable) into ONE ticket/thread so the classifier and the /train reviewer see the whole issue instead of fragments (KNOWN_ISSUES §8/§9 — the user's #1 ask). Migration `014` adds `tickets.last_message_at` + partial index `idx_tickets_grouping` (numbered 014 because 013 was already `bot_replies`; Supabase tracks it as `conversation_grouping`). New pure module `conversation-grouping.ts` (`userThreadText`, `isWithinGroupingWindow`, `groupingCutoffISO`; 22 tests). A grouping branch in `processAndIngestMessage` — inserted after the admin-drop guard and before the new-ticket insert, where control flow guarantees a fresh un-quoted non-admin message — appends `[USER_FOLLOWUP]` blocks and fires `reclassifyGroupedTicket` (a full-thread re-classification that updates category/urgency/summary/product_area/sentiment/suggested_reply but NEVER status — a human/admin owns status; modelled on `reclassifyFromAdminReply`'s 12s race guard, breaker-protected, Pidgin glossary + few-shot inherited). `last_message_at` is stamped on all 6 attach/insert sites so the rolling window stays accurate across user↔admin back-and-forth. `userThreadText` now feeds the full user-side thread into /train + corrections (the 3 review sites), and `originalMessageText`'s regex recognises the new `[USER_FOLLOWUP]` tag. `/api/ingest` gained an optional `senderId` so a test can share (or deliberately not share) a `sender_hash`. Verified: tsc clean, 160/160 tests, and an `/api/ingest` e2e (3 same-sender in-window messages → ONE ticket with 2 `[USER_FOLLOWUP]` blocks, re-classified Withdrawal Issue / High; a 4th out-of-window message → a NEW ticket; re-post idempotent; all test rows cleaned up). Grouping deliberately does NOT change status (out of scope v1), groups into the most-recently-active ticket when a sender has several, and the per-message re-classify is fire-and-forget. **Subtask 7 — /train Skip button — SHIPPED & VERIFIED (2026-06-19).** `TrainCorrectSchema.verdict` now accepts `"skip"`; a skip inserts a `corrections` row with `correction_source:"human_skip"` and `original_category = correct_category = ticket.category` (a human-confirmed no-op that marks the ticket reviewed so it drops out of the /train queue, but never edits the ticket). Both `getFewShotCorrections` and the `/api/verify` selection now filter `.neq("correction_source","human_skip")` so skips are EXCLUDED from few-shot injection and the accuracy run. The /train double-submit guard was broadened from `human_ui`-only to `.in(["human_ui","human_skip"])` so a skipped ticket can't be re-submitted. `src/App.tsx` adds a subtle full-width "⊘ Skip — not a clear support issue" button (`#train-skip`) below the Correct/Wrong row, calling the same `submitVerdict("skip")`. **DB change (explicit user confirmation 2026-06-19):** the live `corrections.correction_source` CHECK constraint only allowed `human_ui`/`admin_reply`, so migration `015_corrections_human_skip` (up/down, applied via Supabase MCP, tracked as `corrections_human_skip`) widens it to add `human_skip` — additive + reversible, existing rows untouched. **Verified e2e on the no-telegram launcher (PORT=3100) against the live DB:** ingested a Withdrawal Issue ticket → `POST /api/train/correct {verdict:"skip"}` returned `{success:true,corrected:false}`; a second skip returned `{alreadyReviewed:true}`; `/api/train/next` no longer returned the ticket; an invalid verdict was rejected `400`; the stored row was exactly one `human_skip` no-op; a filter-mirror query proved 68→67 visible with our skip row hidden; all test rows cleaned (deleting the `messages` row cascaded ticket → corrections, corrections back to baseline 67). tsc clean, 172/172 tests, `npm run build` OK with `#train-skip` bundled into dist. **Conversation grouping (all 7 subtasks) is now COMPLETE.**

## 7. Important Decisions Made & Reasoning
*   **GramJS over Webhooks:** Telegram's MTProto (via GramJS) was chosen because standard bot API webhooks cannot read all group messages unless the bot is an admin with privacy disabled. GramJS acts as a user/client.
*   **Separation of AI Models:** Groq (LLaMA) is used for rapid, cheap classification, while Gemini is reserved for generating nuanced, context-aware suggested replies.
*   **Service Role Key Exclusivity:** The backend is the absolute source of truth. Frontend clients never write directly to Supabase.
*   **Polling over WebSockets (Frontend):** 5-second polling was chosen for the React dashboard for simplicity and resilience in the current deployment constraint, avoiding WebSocket state management complexity on Cloud Run.

## 8. What is Confirmed Working
*   Live Telegram listening and ingestion via GramJS.
*   Groq LLaMA classification into strict JSON.
*   Supabase data insertion bypassing RLS.
*   Frontend dashboard rendering, data fetching, and UI updates.
*   Demo Mode toggling.
*   Re-processing the same Telegram message is now idempotent (verified 2026-06-11: "Skipping duplicate telegramId" fires; replies never duplicate).
*   KPI cards verified against ground-truth DB counts (2026-06-11): Active, In Review, Resolved (Dismissed excluded), Resolution Rate 53%, with `resolved_at` stamped on every resolution path and Lagos-timezone day boundaries.
*   Every dashboard filter (search, category, urgency, date, custom range) updates the KPI cards together with the table (verified 2026-06-11 against a UTC server).
*   Unquoted admin replies attach to the right ticket via the 90-second window heuristic and mark it "In Review" (verified end-to-end 2026-06-11).
*   Production build artifact verified locally (2026-06-11): `npm run build` then `node dist/server.mjs` boots, connects to Telegram, and answers `/api/health` ok — this is exactly what Railway runs per `railway.toml`.
*   **Milestone 3 (2026-06-12): The Human Loop — corrections table, admin-reply re-classification, few-shot learning, /train interface.**
    *   **`corrections` table (migration 010, applied with explicit confirmation):** every human fix to an AI classification is stored — `ticket_id`, `message_text` (snapshot of the original user message, clean of reply blocks), `original_category`, `correct_category`, `corrected_by`, `correction_source` (`human_ui` | `admin_reply`), `created_at`. A row with `original_category = correct_category` means a human reviewed and CONFIRMED the AI — that is how /train marks tickets "reviewed" without a second table. RLS enabled (service role bypasses). Migration 011 also enabled RLS on `learned_keywords` and `filtered_messages` (Supabase security advisory, both approved).
    *   **Admin-reply re-classification:** both admin-reply attachment paths (quoted and 90-second window) fire `reclassifyFromAdminReply`, which asks Groq whether the admin's reply contradicts the assigned category. It updates ONLY `category` + `updated_at` (never status — cannot un-escalate/reopen/resolve), requires an exact case-insensitive category match from the model (no normalizeCategory fallback that could default to General Question), waits 12s and re-checks once if classification is still pending, and records the correction with source `admin_reply`. Verified live: "Deposit Issue" → "Account Access" on a contradicting reply; a generic "we are looking into it" reply changed nothing.
    *   **Few-shot injection:** before each classification, `getFewShotCorrections` extracts keywords (≥4 chars, stopwords removed, max 12) from the incoming message, scores the 200 most recent corrections by keyword overlap (deduped newest-first per message), and appends the top 5 to the system prompt as worked examples. Applied to the main Groq classification, the Gemini fallback (same block, fetched once), and `/api/test-message`. **`/api/eval` deliberately does NOT inject** so the benchmark stays a comparable raw-model baseline. Verified live: after one stored correction, a similar "money never enter" message flipped from Deposit Issue to Account Access in both the test panel and the real ingestion pipeline; a dissimilar message got no injection.
    *   **/train interface:** separate route (no client router — the SPA fallback serves any path; `App.tsx` branches on `window.location.pathname`). `GET /api/train/next` pages newest-first through classified non-admin tickets and returns the first with no corrections row, plus the category list and counts. `POST /api/train/correct` (Zod-validated) stores the verdict; "wrong" also updates the ticket category and writes an audit log entry; a per-ticket `human_ui` double-submit guard returns `alreadyReviewed`. Dashboard polling is disabled on /train. Verified end-to-end with API round-trips and UI screenshots; all test data cleaned up. **Discoverability + context (2026-06-13):** the main dashboard header now has a "🎓 Train the AI" button linking to /train, and the /train page opens with a one-line explanation of the task. The reviewer also gets conversation context — a "Show full conversation" toggle (the full `raw_text` thread, reply blocks included) and a "View on Telegram" deep link — because reviewing an isolated message with no surrounding thread is hard to judge. **Important: /train needs 20–30 human reviews before training-loop verification shows meaningful results** — so far only 1 ticket has been reviewed (possibly via automated testing), which is why the conversation-context aids were added.
*   **Milestone 4 (2026-06-12): Hardening + Measurement.**
    *   **"Verify" accuracy function (PRD Feature 2's last acceptance criterion):** `POST /api/verify` starts a background run (same job pattern as backfill; `GET /api/verify/progress` polls). It re-classifies the most recent human-reviewed messages from `corrections` twice each — once raw (no few-shot, the baseline) and once with few-shot injection under **leave-one-out** (`getFewShotCorrections` gained an `excludeMessageText` param so a message can never see its own stored answer). Sequential Groq calls with 2.1s spacing; max 50 cases per run (default 20, newest distinct messages, latest verdict wins). The Benchmark modal gained a "Training Loop Verification" section: progress bar, baseline vs with-training vs points-gained cards, a human-fix-only breakdown line, and a per-message table. Verified live: with 3 reviews stored (2 fixes, 1 confirm), baseline 33% → with-training 100% (+67 points; 0% → 100% on the human-fixed subset), logs confirmed exactly the cross-message injections expected and zero self-leak.
    *   **DB-side KPI aggregation (migration 012, applied):** `tickets_stats(...)` Postgres function computes every KPI over the FULL filtered set plus a `volumeByDay` series (per-Lagos-day counts). `/api/tickets` now calls it via RPC instead of pulling ≤5,000 rows into JS; `rawStatsData` is gone from the payload and the frontend volume chart reads `volumeByDay`. Filter parity proven live across 7 filter combinations (all-time, fixed custom window, search, urgency, issues-only, last-7, category) — every scalar, the category breakdown, and the day series matched the old implementation exactly at 824 tickets.
    *   **Gemini suggested-reply retry + repair sweep:** `generateSuggestedReply` retries capacity errors (429/5xx/"high demand"/timeouts — `isRetryableLLMError`) up to 3 attempts with exponential backoff + jitter; each attempt is its own breaker call and a breaker-open fast-fail stops retrying. Backstop: every 15 minutes a sweep finds classified ACTIVE tickets from the last 24h with no suggested reply (max 10/run, update guarded on still-null) and regenerates — verified live by nulling a fresh ticket's reply and watching the sweep refill it; also verified the sweep matches 0 of the 661 historical null-reply tickets (all resolved/dismissed/old).
    *   **Classification race fixed:** the background classifier's final write is now a guarded two-step — update INCLUDING status only where status still equals what ingestion inserted (atomic; WHERE re-evaluated under the row lock), else write classification fields WITHOUT status. Applied to all three sites (Groq success, Gemini fallback, failure path). Verified live: a ticket flipped to "Escalated" mid-classification kept "Escalated" while category/summary/reply still arrived; an untouched Critical ticket still got "In Review" + [ESCALATED].
*   **Milestone 5 (2026-06-12): Automated Status Update Bot (PRD Feature 4) — built, verified in dry-run, ships OFF.**
    *   **What it does:** when an admin changes a ticket to **Resolved / Escalated / Awaiting User** in the dashboard (`POST /api/tickets/:id/status` — the ONLY trigger; ingestion-driven status changes never post), GramJS replies to the user's original message in the group with a fixed empathetic template (neutral voice, no team signature — deliberate, since the session is a personal account). Open / In Review / Dismissed never notify.
    *   **Safety rails (all proven locally, 18/18 suite + 2/2 kill-switch assertions, Telegram disconnected):** `BOT_REPLIES_ENABLED` kill switch (default OFF — not even logs); `BOT_REPLIES_DRY_RUN` (default ON — full pipeline runs, logs "WOULD send" and records a `bot_replies` audit row, Telegram never called); claim-then-send dedup via partial unique index on `(ticket_id, status) WHERE dry_run = false` (at most one live reply per ticket per status, EVER — re-resolving after a reopen stays silent); rate limit 5s min gap / 20 per hour (dry-run consumes it too, as a faithful rehearsal); skips tickets older than 7 days (backlog cleanup stays silent), admin-authored tickets, tickets without a `telegram_message_id`, and any group other than `TELEGRAM_GROUP_USERNAME`; failed sends are recorded in `bot_replies` and never auto-retried (v1).
    *   **Self-ingestion guard:** the bot's own replies would otherwise be re-ingested as admin replies (ADMIN_REPLY block, `first_admin_reply_at` stamp → corrupting Avg Response Time, Groq reclassification on template text). `processAndIngestMessage` now skips any telegram id found in an in-memory Set seeded at startup from the last 24h of `bot_replies.sent_telegram_message_id` (AutoFetch only looks back 2h). The seeding runs even when the kill switch is off — on purpose, so replies sent before a shutdown stay guarded.
    *   **Rollout state (2026-06-12 evening): Phase A DONE. Phase B DONE & VERIFIED** — enabled on Railway with dry-run on; a real dashboard status change produced exactly one `bot_replies` audit row with the right target message (verified against the `messages` table), the right group, and the character-exact template; all rails held. **Phase C ATTEMPTED & BLOCKED:** the one controlled live send failed with `400: USER_BANNED_IN_CHANNEL` — the GramJS session account can READ the group (listener unaffected) but is banned/muted from SENDING in it. Nothing was posted; the failure was recorded cleanly (claim-then-send worked as designed). **Feature is now PARKED:** `BOT_REPLIES_ENABLED=false` on Railway (and remove the `BOT_REPLIES_DRY_RUN=false` var so a future re-enable starts back in dry-run). Note: the failed row permanently blocks the pair (ticket `0c72808f-1112-4dd7-aaa1-ed5ad2f9e938`, "Awaiting User") — delete that `bot_replies` row before any retry. To revive the feature: get the account unmuted by group admins, or switch to an account with send rights (different session string — never regenerate without explicit instruction). Lesson: dry-run can never catch Telegram-side permission errors, because it skips the API call.
*   **Milestone 2 (2026-06-12): 4-state resolution workflow + Avg Response Time.** Statuses are now Open / In Review / Escalated / Awaiting User / Resolved / Dismissed (DB CHECK constraint widened, migration 009). Workflow rules: admin reply moves Open → In Review but never un-escalates (Escalated / Awaiting User keep their state); a user reply to an Awaiting User ticket flips it back to In Review; all four active states accept replies and count as Active in the resolution rate. New `first_admin_reply_at` column is stamped once per ticket (with the reply message's own timestamp) by both admin-reply paths; the dashboard has a new Avg Response Time KPI card (legacy tickets are null and excluded). The stats payload gained `inReviewCount`, real `escalatedCount`, `awaitingUserCount`, `avgResponseMs`, `respondedCount` — the "In Review" card previously read a field misleadingly named `escalatedCount`. `/api/ingest` (super_admin) now also accepts `replyToMsgId` for simulating quoted reply threads. Everything verified end-to-end on a local server against the live DB (status round-trips, both reply paths, re-ingest idempotency, 23s measured avg response) and all test data deleted afterwards.

## 8b. Deployment (Railway — production)
*   **Production URL: `https://quidax-telegram-gemini-production.up.railway.app/`** (verified healthy 2026-06-12: `/api/health` returned ok, `telegramConnected: true`, all circuit breakers closed).
*   **Decision (2026-06-12): staying on Railway.** The Fly.io migration was abandoned because Fly requires a payment method; `fly.toml`, `Dockerfile`, and `.dockerignore` were removed from the repo (commit `510cb0c`). If hosting is revisited, that commit has the full working Fly config and section 8c/8d of this doc's git history has the runbook.
*   `railway.toml` at the repo root: builds with `npm run build`, starts with `node dist/server.mjs` (NOT `npm start` — that script contains a Windows-only `chcp` command), health check `/api/health` (5-min timeout for Telegram connect), `numReplicas = 1` pinned because each replica would open its own GramJS session and double-ingest.
*   Railway injects `PORT` automatically — never set it manually.
*   Env vars to set in the Railway dashboard (values from local `.env`): `GROQ_API_KEY`, `GEMINI_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `DASHBOARD_PASSWORD`, `TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `TELEGRAM_SESSION_STRING`, `TELEGRAM_GROUP_USERNAME`, `TELEGRAM_ADMIN_USER_IDS`, `TELEGRAM_CONNECT_DELAY_MS=60000` (rolling-deploy session-overlap guard — see §9), `CHANNEL_DIFF_ENABLED=true`, `NODE_ENV=production`, `DEMO_MODE=false`; optional: `JIRA_BASE_URL`, `JIRA_EMAIL`, `JIRA_API_TOKEN`, `JIRA_PROJECT_KEY`, `HEARTBEAT_URL`, `SUPPORT_API_KEY`; **outbound bot (Milestone 5, PARKED 2026-06-12 — keep `BOT_REPLIES_ENABLED` unset/false):** `BOT_REPLIES_ENABLED` (kill switch, default off) and `BOT_REPLIES_DRY_RUN` (default true; `false` makes the bot post to the real group). Phase C was blocked: the session account is banned from sending in the group — see Milestone 5 rollout state above before ever re-enabling. **Key rotation note (2026-06-12):** `SUPABASE_SERVICE_ROLE_KEY` now holds a new-format `sb_secret_…` key (named `railway_backend` in Supabase); the legacy JWT keys are disabled and the leaked key was verified dead (401). NEVER re-enable legacy API keys in the Supabase dashboard — that resurrects the leaked key.
*   Railway free tier is a $5/month credit (~16 days of runtime) — watch the balance; deploys after the credit runs out will need a paid plan.

## 8c. Local testing (safe — no Telegram session)
*   Run `npx tsx scripts/dev-no-telegram.mjs` (gitignored like all `*.mjs` scripts; recreate from this note if missing — it sets `TELEGRAM_SESSION_STRING=""` and `PORT=3100` before importing `server.ts`, and dotenv never overrides existing env vars).
*   This keeps the GramJS listener OFF so production on Railway stays the only live Telegram session, while the local server still uses the live Supabase DB, Groq, and Gemini.
*   `.claude/launch.json` has a `pulsedesk-local` preview config that uses this launcher.
*   PowerShell gotcha: `$env:X = ""` DELETES the variable instead of setting it empty — use the launcher script (or Git Bash `X="" command`) to blank an env var.

## 9. Suspected Broken, Untested, or Needs Verification
*   **FULL AUDIT 2026-06-12 (evening): see KNOWN_ISSUES.md section 6 — START THERE.** Confirmed: the live GramJS listener has ingested nothing for days (AutoFetch sweeps are the only ingestion path, with silent drops, newest-first ordering corruption, and a watchdog blinded by non-group chats); quoted user messages with no matching ticket are dropped without a trace; "General Question" auto-dismissal swallows every misclassified real issue; the Raw edit/delete handlers have no group filter and have provably corrupted rows via cross-chat message-ID collisions; benchmark_cases.json never deploys (gitignored); Gemini suggested replies have been failing since ~21:34Z Jun 12. Fix order, evidence, and the approved auto-dismiss policy change are all in KNOWN_ISSUES.md section 6.
    *   **Fix-phase progress:** Fix 1 (silent drops, `586dedf`), Fix 2 (cross-chat edit/delete, `048cf40`), Fix 3 (AutoFetch oldest-first, `990e1cd`), Fix 4 (auto-dismiss policy, `0494a1a`), Fix 5 (quoted admin reply retro-ingests its missing parent, `e848d5f`), Fix 6 (live-listener resurrection + watchdog, `1a7130d` — audit item 1, the root cause the others mitigated), Fix 7 (benchmark ships to prod as a bundled TS module, `c8b2ea9` — verified live, `/api/eval` returns total:12), and Fix 8 (Gemini quota-aware backoff so the repair sweep stops re-burning an exhausted quota, `7a09b15`) are DONE. **All §6 items 1–8 are now addressed in code.** Verification pass 1 (2026-06-13): **item 8 is now VERIFIED unblocked** — a direct DB query found 5 `corrections` rows (all Jun 13), 4 from real `admin_reply` on real group tickets (telegram ids 139613/139614/139620, e.g. the iPhone-app question with two `[ADMIN_REPLY]` blocks and `first_admin_reply_at` stamped). Still awaiting live-log confirmation, not new code: **Fix 6 (item 1)** — the watchdog-trip half was confirmed, but the live-delivery half was NOT, and Fix 10 (2026-06-13, `14abd74`) found why: the listener never primed the GramJS session with `getDialogs()`, so on a push-only update system the supergroup's messages were never delivered (fresh DB proof: min ingest lag 185.8s, all AutoFetch-batched). Fix 10 adds startup/reconnect priming + a membership-revealing log line; it is deployed and now awaits the production verdict — either `target group present in dialogs` + the first `Live message received`/`matchedBy:"channelId"` line (→ CLOSED), or `TARGET GROUP IS ABSENT` (→ the account is banned/not a member, a Telegram-account swap is required, no further code fix possible). **VERDICT (2026-06-14, `a1c3958` logs via `railway logs`): neither clean outcome — `target group present in dialogs` (exact channelId 2129818880) on every startup + reconnect, so the account IS a member (NOT the ban), yet the live listener still delivered NOTHING (zero `Live message received` in 12h, min DB lag 193.8s, all AutoFetch-batched). Fix 10's priming hypothesis is DISPROVEN — necessary-looking but NOT sufficient; the live-push root cause is deeper in GramJS and is now PARKED as a research spike. Fix 11 (2026-06-14) responded by making AutoFetch the primary path (15→3 min + batched pre-dedup) and killing the watchdog's every-5-min reconnect loop; live-listener delivery remains OPEN.** **Fix 8 (item 7)** — inconclusive so far (the reviewed Railway logs predate the Fix 8 deploy and captured no sweep/Gemini-error line; the `gemini` breaker is currently CLOSED, so the failure may have abated as the daily quota reset). Needs a sweep on the `7a09b15` build during a window with a qualifying ticket to capture the `describeLLMError` output.
*   **GramJS Session String — ROTATED TWICE (sessions #1→#2 on 2026-06-15, #2→#3 on 2026-06-16).** Session #1 was revoked externally (`AUTH_KEY_DUPLICATED` on every connect; ingestion dead ~20h from 06-14 17:28Z). Rotated to session #2 on 2026-06-15 on a NEW personal Telegram account. Session #2 was then burned by a Railway rolling-deploy session-overlap on 2026-06-16 (see the rolling-deploy entry below): the new container connected while the old still held the session, causing Railway Telegram to permanently invalidate session #2. Rotated to session #3 on 2026-06-16 — a third personal account, normal group member. Generated with `node -r dotenv/config scripts/generate-session.cjs` (CommonJS; renamed `.js`→`.cjs` because the project is `"type":"module"`). **Session #3 verified live (2026-06-16 ~14:09Z, commit `3a7da84`):** `Delaying initial connect 60s…` then `✅ Connected (startup attempt 1)` with ZERO AUTH_KEY_DUPLICATED errors, primed + `Seeded channel pts 215610`, fresh messages ingesting at ~37s lag, `telegramReady:true`, `/api/health` commit `3a7da84`. **Open follow-ups:** (a) whether session #3's account can SEND to the group is UNTESTED — Milestone 5 is still parked; (b) session-expiry longevity unverified.
*   **Rolling-deploy session-overlap fix (2026-06-16, commits `0bd0dbb` + `3a7da84`).** Root cause: Railway rolling deploy starts the new container, which passes `/api/health` immediately (Express boots before Telegram), then Railway tears down the old container. If the new container's `client.connect()` fired during the overlap, Telegram responded `406: AUTH_KEY_DUPLICATED` and permanently burned the session string. **Fix — two parts both shipped:** (A) new pure module `deploy-overlap.ts` — `resolveConnectDelayMs()` parses `TELEGRAM_CONNECT_DELAY_MS` (default 60s, max 5 min; "0" disables — for a cold first deploy with no old container); the server delays only the INITIAL connect, not reconnects; 12 unit tests; (B) SIGTERM/SIGINT graceful-disconnect handler in `server.ts` — when Railway sends SIGTERM to the old container, `client.disconnect()` is called (raced against 3s timeout) before `process.exit(0)`, ensuring the session is cleanly released before the new container connects. Both defenses are required: delay alone fails if the old container is slow; SIGTERM alone fails if it arrives after the new container connects. **`TELEGRAM_CONNECT_DELAY_MS=60000` is now set in Railway Variables and must remain.** Set it to "0" only on a cold first deploy. Tests: 12 unit tests in `tests/deploy-overlap.test.ts`, tsc clean. Verified in production: connected on startup attempt 1 with no AUTH_KEY_DUPLICATED errors.
*   **High-Volume Concurrency:** The async ingestion handles moderate loads, but behavior under extreme spikes (e.g., thousands of messages per minute) is untested.
*   **Rate Limit Edge Cases:** The `heavyLimiter` might trigger false positives if the Cloud Run proxy headers are misconfigured.
*   **Keyword Learning System:** Basic logic exists but long-term effectiveness is unverified.

## 10. Repeatedly Revisited / Fixed Multiple Times
*   **Schema Validation:** The LLM hallucinating category strings or casing has required multiple patches (strict prompting, Zod integration, fallback normalization).
*   **Project Context Wipes:** The AI environment frequently lost context, requiring aggressive tracking files and ignore-file management to prevent artifacts from confusing the agent.

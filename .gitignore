node_modules/
build/
dist/
dist_temp/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Temporary backups and generated files
*.recovered
*.recovered2
*.reconstructed
server_from_git.ts
*.txt
*.jsonl
benchmark_cases.json
history_summary.json

# Test and diagnostic scripts
*.mjs
*.cjs
# ...except the Telegram session generator, which is a real auth utility (it is
# .cjs because the project is "type":"module" and the script is CommonJS).
!scripts/generate-session.cjs
# ...and the accuracy-audit samplers, which are real methodology utilities kept
# in the repo so the audit is reproducible (their scorers are .ts, committed).
!scripts/audit-sample.mjs
!scripts/audit-system.mjs
test*.js
fix*.js
check*.py

# Claude Code local-only settings (personal permission allowlist)
.claude/settings.local.json

# Accuracy-audit working files (real message text → kept off git; only the
# rolled-up numbers in audit-results.json are committed)
audit/

# Personal pitch context (contains personal email/background details)
PITCH_CONTEXT.md

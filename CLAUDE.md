# PulseDesk Agent Rules

## Stack
Node.js + Express + TypeScript (server.ts), React 19 + Vite + Tailwind v4 (src/App.tsx), Supabase service role key only, Groq for classification, Gemini for replies, GramJS for Telegram.

## Before every task
Read PULSEDESK_HANDOFF.md and PRD.md first. Restate the task, list which files will change, identify risks. Wait for approval before acting on anything non-trivial.

## Rules
- Smallest change only — never touch unrelated files
- Never use SUPABASE_ANON_KEY for backend operations
- Never expose API keys to the frontend
- Never change DB schema without explicit confirmation
- Never regenerate the GramJS session string without explicit instruction
- One task at a time — complete and confirm before moving to the next
- All Git and PR descriptions in plain English a non-developer can understand

## End of every session
Update PULSEDESK_HANDOFF.md, KNOWN_ISSUES.md, and PRD.md. Summarize what was done and what is pending.

## Lessons learned
- **Local test runs on port 3100 can leave background processes holding the port between bash calls** — if a test server seems missing or curl hangs, always check for and kill lingering node processes on that port before starting a new one (PowerShell: `netstat -ano | Select-String ":3100"` → `Stop-Process -Id <pid> -Force`).
- **Three overlapping ingestion paths:** live listener, AutoFetch (startup + every 15 min, 2-hour lookback), and manual backfill all call `processAndIngestMessage`. Any mutation inside that function MUST be idempotent — the `telegram_message_id` dedup check must stay at the very top, before any branch that writes.
- **`tickets.updated_at` is NOT auto-maintained** — there is no DB trigger; code must set it explicitly on every update. `resolved_at` is the source of truth for when a ticket was closed; reopening clears it. Legacy tickets resolved before 2026-06-11 have `resolved_at = null` by design.
- **Resolution Rate definition (product decision, 2026-06-11):** Resolved ÷ (Resolved + Active). Dismissed tickets are spam/chatter, never counted as resolutions anywhere.
- **"Today" KPIs use Africa/Lagos**, not server time (Railway runs UTC).
- **The reconstructed migration files do not match the live schema** — always verify columns against the live DB before trusting `supabase/migrations/`.
- **Local testing runs on PORT=3100** (port 3000 is occupied by an unrelated MCP tool). Shell env vars override `.env` because dotenv does not override existing vars.
- **`/api/tickets` filter split:** `applyBaseFilters` (group, issues_only, dates, search) is shared by the table and stats queries; `status` is deliberately table-only because the KPI cards break tickets down by status. All date boundaries are Lagos calendar days — never use server-clock midnight.
- **`/api/ingest` (super_admin) accepts `isAdmin`, `msgDate`, and `replyToMsgId`** — use it to simulate admin replies, historical timestamps, and quoted reply threads when testing ingestion heuristics end-to-end. Always clean up test tickets/messages by `telegram_message_id` afterward (deleting the `messages` row cascades to the ticket via FK), and guard against attaching test admin replies to real live tickets (check no newer Open ticket exists first).
- **4-state workflow semantics (Milestone 2, 2026-06-12):** Active statuses are Open / In Review / Escalated / Awaiting User. An admin reply moves Open → In Review but NEVER changes Escalated, Awaiting User, or Resolved; a user reply to an Awaiting User ticket flips it to In Review; all four active states accept replies and count in the resolution-rate denominator. `first_admin_reply_at` is stamped exactly once, with the reply message's own timestamp (not now()), so backfills stay accurate — never overwrite it.
- **Production runs only ONE GramJS session at a time.** Railway and Fly.io must never both be live (the `telegram_message_id` dedup check is not concurrency-safe; only the DB UNIQUE constraint catches the race). Stop the old platform before deploying the new one — startup AutoFetch's 2-hour lookback recovers messages missed during the gap. Same caution applies to local testing while production is live: keep the window short.
- **Railway runs `node dist/server.mjs` via railway.toml, not `npm start`** — the npm script has a Windows-only `chcp` command. Keep `numReplicas = 1`: each replica opens its own GramJS session and double-ingests.
- **Async classification can overwrite statuses set by reply-attachment** (~5-10s race): if an admin reply lands before Groq classifies the parent ticket, "In Review" can be clobbered back to "Open"/"Dismissed". Known, documented in KNOWN_ISSUES.
- **Corrections semantics (Milestone 3, 2026-06-12):** a `corrections` row with `original_category = correct_category` means a human CONFIRMED the AI — that is how /train marks tickets reviewed; any corrections row (either source) makes /train skip the ticket. `reclassifyFromAdminReply` requires an exact case-insensitive category match from the model — never reintroduce a `normalizeCategory` fallback there, it would default hallucinations to "General Question" and rewrite real tickets.
- **Bug 4 / auto-resolve (2026-06-14):** `reclassifyFromAdminReply` NO LONGER touches only `category`. Its Groq prompt (`RECLASSIFY_SYSTEM_PROMPT`) now also returns `{resolved: bool}`, and an affirmative/definitive admin answer ("Yes, you can…", "It's done") auto-resolves the ticket. The status write is GUARDED by `shouldResolveFromAdminReply` (pure module `admin-reply-resolution.ts`) to active queue states ONLY — `Open`/`In Review` (the `AUTO_RESOLVABLE_STATUSES` array) — and applied as a CONDITIONAL update (`.in("status", AUTO_RESOLVABLE_STATUSES)`) so it can NEVER un-park an `Escalated`/`Awaiting User` ticket a human set, nor re-touch `Resolved`/`Dismissed`, nor lose a concurrent dashboard change (same defense as the Milestone 4 race fix). `resolved_at` is stamped on auto-resolve. The category branch was restructured to log-and-continue (not early-return) so a category hiccup never blocks the resolve. `resolved` is strict-true only (a missing/garbage field never resolves). Bug 4 also added a feature-existence rule to `GROQ_SYSTEM_PROMPT` ("can I…?"/"is it possible to…?" with no problem → General Question, not Trading/Withdrawal/Deposit) + benchmark cases 19-20 — base-prompt, NOT few-shot, so it reaches `/api/eval` like the Pidgin glossary.
- **Bug 3 / admin-message drop (2026-06-14, `cda3ebc`):** `checkIsAdmin` runs correctly on all ingestion paths — admin messages are now dropped when unattached (never become tickets). Don't re-investigate detection if you see admin-related issues; check the downstream drop guard in `admin-message-policy.ts` (`disposeUnattachedMessage`, called before the ticket insert in `processAndIngestMessage`) instead.
- **Few-shot injection lives in the main pipeline, the Gemini fallback, and `/api/test-message` — but deliberately NOT `/api/eval`**, which stays a raw-model baseline for comparable benchmark numbers. Deleting a ticket cascades its corrections (FK), so cleaning up test tickets also removes their few-shot examples — always verify the `corrections` count after test cleanup.
- **`*.mjs` is globally gitignored** (one-off test scripts convention). `scripts/dev-no-telegram.mjs` is therefore untracked — it blanks `TELEGRAM_SESSION_STRING` and sets PORT=3100 before importing server.ts, making local testing safe while Railway production is live. PowerShell cannot set an env var to empty (`$env:X=""` deletes it) — use that launcher or Git Bash.
- **Hosting decision (2026-06-12): staying on Railway** — Fly.io requires a payment method. The Fly config was removed in commit `510cb0c`; recover it from git history if ever needed.
- **Local server serves a STALE `dist/` build if one exists** — server.ts prefers `dist/index.html` over Vite middleware, so frontend edits are invisible until `npm run build`. When verifying UI changes locally, rebuild first (a browser reload then picks up the new bundle; the server reads dist from disk per request, no restart needed).
- **KPI stats come from the `tickets_stats` Postgres function (migration 012, Milestone 4)** — `/api/tickets` passes the base filters as RPC params and merges the returned jsonb into the stats payload; `resolutionRate`/`resolutionData`/`totalCount` are still computed in JS. The volume chart reads `stats.volumeByDay` (Lagos-day buckets); `rawStatsData` no longer exists. Any new KPI must be added inside the SQL function, not as a JS row-scan, and any new base filter must reach BOTH the table query and the RPC params.
- **`/api/verify` (Milestone 4) measures training-loop accuracy with leave-one-out** — `getFewShotCorrections(supabase, text, excludeMessageText)` skips rows matching `excludeMessageText` so a verified message never sees its own stored correction. Never drop that third argument from the verify path: without it the few-shot pool leaks the answer and the "with training" score is meaningless.
- **The async classifier writes status through a guarded conditional update (Milestone 4)** — `applyClassification` updates WITH status only where `status = insertedStatus` (atomic), else writes fields without status. All three classifier write sites (Groq, Gemini fallback, failure) must keep using it; a plain `.update({status})` there reintroduces the race where the classifier clobbers "In Review"/"Escalated" set during the ~5-10s window.
- **`isRetryableLLMError` treats `[Timeout]` from `withTimeout` as retryable** — discovered live when the first sweep test timed out and was wrongly classified non-retryable (regex said "timed out", our wrapper throws "[Timeout]"). Retry only 429/5xx/capacity/timeout errors; never a breaker-open fast-fail ("[CircuitBreaker]"), never other 4xx.
- **`messages.telegram_message_id` is BIGINT in the live DB while `tickets.telegram_message_id` is TEXT** — another reconstructed-migrations mismatch. Raw SQL against `messages` needs numeric literals or casts; supabase-js string filters work because PostgREST casts.
- **The reply-repair sweep (every 15 min) only touches tickets that are <24h old, ACTIVE status, classified, non-admin, with `suggested_reply IS NULL`** — and its update is guarded on still-null so it never overwrites an agent's edit. The bound was verified against live data (0 of 661 historical null-reply tickets matched). Widening any of those bounds risks a Gemini-quota burn on history.
- **Outbound bot rails (Milestone 5, 2026-06-12):** `maybeSendStatusBotReply` is the ONLY code that posts to the group, triggered ONLY by `POST /api/tickets/:id/status`. Both env flags must stay fail-safe: `BOT_REPLIES_ENABLED` defaults OFF, `BOT_REPLIES_DRY_RUN` defaults ON (`!== "false"`). Dedup is claim-then-send: a `bot_replies` row (result `pending`) is inserted BEFORE Telegram is called, and the partial unique index `(ticket_id, status) WHERE dry_run = false` is the concurrency-safe gate — never switch to select-then-insert. Dry-run rows never block live sends; failed rows DO block forever (v1: no auto-retry; delete the row manually to allow one).
- **The self-ingestion guard in `processAndIngestMessage` must never be removed:** bot replies skip the `messages` insert, so the normal telegram-id dedup can NOT catch them — without the `botSentMessageIds` check (seeded at startup from the last 24h of `bot_replies`, even when the kill switch is off) the bot's own replies re-ingest as admin replies, stamp `first_admin_reply_at`, and corrupt the Avg Response Time KPI.
- **Testing gotcha: an unquoted admin message sent via `/api/ingest` attaches to any non-admin active ticket created within the 90s window before its `msgDate`** — it returns the PARENT ticket, not a new one. To create a standalone admin ticket in tests, backdate `msgDate` to a window verified empty in the live DB (and keep it under any age guard you're testing). Also: `/api/ingest` sits behind `heavyLimiter` (5 requests / 15 min, in-memory) — restart the local server to reset it between test runs.
- **Milestone 5 is PARKED (2026-06-12): the GramJS session account is BANNED FROM SENDING in OfficialQuidaxCommunity** (`400: USER_BANNED_IN_CHANNEL` on the first live send; reading/ingestion unaffected). Keep `BOT_REPLIES_ENABLED=false`. Dry-run can structurally never catch Telegram-side permission errors — it skips the API call — so any future go-live must budget for a controlled live test. A `failed` bot_replies row blocks its (ticket, status) pair forever: (`0c72808f…`, "Awaiting User") is burned; delete the row before retrying. Never triage tickets with live mode on while sends are failing — each status change burns another pair.
- **Supabase key rotation (2026-06-12, done & verified):** production uses a new-format `sb_secret_…` key (named `railway_backend`) in the `SUPABASE_SERVICE_ROLE_KEY` var; supabase-js takes it as a drop-in. Legacy JWT keys (anon + service_role) are DISABLED — **never re-enable legacy API keys in the Supabase dashboard; that resurrects the leaked key** from public git history (verified dead, 401). The anon key was never used by production code. Also: `messages` has NO `created_at` column — use `ingested_at`/`message_timestamp` (another live-schema-vs-migrations mismatch).
- **The cross-network leg of a fix can't be verified on the no-telegram launcher** (`tlClient` is null there, so anything calling `client.getMessages`/the live `NewMessage` handler never runs locally). Pattern (Fix 5, 2026-06-13): split the orchestration into a PURE, injected module with its own vitest file (like `telegram-guards.ts` / `classification-policy.ts` / `quoted-parent.ts`), unit-test every branch with fakes, and guard the live call on `tlClient` so it's a safe no-op locally. Accept that the live leg's FIRST real exercise is production — say so honestly in the handoff rather than claiming it verified.
- **The watchdog's `lastMessageReceivedAt` clock must only be stamped on TARGET-group messages (Fix 6, 2026-06-13, `listener-health.ts`).** The original handler stamped it before the in-group check, so DMs/other chats the session account is in kept the 30-min reconnect watchdog asleep while the group listener was silently dead — and `/api/health` looked healthy (a "now" timestamp) the whole time. Never move that stamp back above the `isMessageInTargetGroup` check. Group membership is now an EXACT resolved-channel-id match (reuse `resolveTargetChannelId`), with the old username/title/"quidax" substring guess kept ONLY as a startup fallback while the id is still unresolved (fail-safe toward ingesting, since ingestion has its own group rail). `lastMessageReceivedAt` is now a meaningful liveness signal — if it stops advancing, the live listener is genuinely down.
- **Anything `/api/eval` (or any deploy) needs at runtime must be a committed, non-ignored file — `benchmark_cases.json` was gitignored and never reached Railway (Fix 7, 2026-06-13, `c8b2ea9`).** The 12 gold benchmark cases now live in `benchmark-cases.ts` (a committed TS module bundled into `dist/server.mjs`), the single source of truth shared by `/api/eval` and the `eval.ts` CLI — never move benchmark data back into a gitignored JSON. The eval loop spaces Groq calls 2.1s apart (free-tier rate limit, like the verify loop); accuracy math returns `null`, never NaN, when there are no cases; the modal shows a "No benchmark cases available" empty state rather than a bare "%". `/api/eval` stays a RAW-model baseline — never add few-shot injection there. The Pidgin classification gap (§3) was closed by Fix 9 — `benchmark-cases.ts` now has 18 cases (12 English + 6 Pidgin, ids 13-18).
- **Pidgin classification uses the base system prompt, not few-shot corrections (Fix 9, 2026-06-13, `0354351`, `pidgin-glossary.ts`).** Nigerian Pidgin English phrases like "e don do", "dem block my account", "money never enter", "una too much" were falling back to General Question or getting wrong urgency. Fix: `PIDGIN_GLOSSARY_PROMPT` (phrase-mapping + 5 worked examples in the system prompt, different wording from the benchmark cases) is appended to `GROQ_SYSTEM_PROMPT` in server.ts. This is the ONLY knob that changes both `/api/eval` (raw-model baseline) and the live pipeline simultaneously — few-shot is NOT the right tool for language coverage because it requires stored corrections to fire. The benchmark was extended to 18 cases (6 new Pidgin); proven 100% Pidgin accuracy after vs 67% before. Future language-coverage gaps (e.g. Yoruba, Hausa) should follow the same pattern: a glossary section in `GROQ_SYSTEM_PROMPT`, not few-shot examples.
- **A quota/429 error is "retry much later", not "retry in a second" — don't let `isRetryableLLMError` treat them the same (Fix 8, 2026-06-13, `7a09b15`, `gemini-quota.ts`).** Gemini daily free-tier quota exhaustion was making `generateSuggestedReply` fire 3 real calls per ticket (429 counts as "retryable" → short-backoff retry), tripping the shared `geminiBreaker` (threshold 3) on every 15-min repair sweep and re-burning the already-dead quota (breaker recovers in 30s, so the next sweep re-burned again). Fix: `isQuotaExhaustedError` (429 / RESOURCE_EXHAUSTED / "quota" — but NOT a transient 503/overload, which still earns a short-backoff retry) arms a 60-min cooldown; while cooling down, `generateSuggestedReply` returns immediately after 1 failing call and the sweep skips with one log line, so nothing re-burns the quota and the breaker stays closed for the Gemini classification-fallback path. The cooldown is scoped to `generateSuggestedReply` only (the rare fallback call site is untouched — smallest change). **Always log the FULL LLM error detail (`describeLLMError`: status/statusText/errorDetails), never just `e.message`** — the original retry line logged only the message, so the failure mode (quota vs. bad model name vs. overload) could only be guessed. The reply model is `gemini-3.5-flash` while the classification fallback is `gemini-2.5-pro`; if a sweep's logged error is a 404 NOT_FOUND rather than a 429, the model name is the real bug, the cooldown (correctly) never engages, and a model change is the follow-up.
- **GramJS 2.26.x is PUSH-ONLY for updates — a `StringSession` client must be primed once with `client.getDialogs()` after connecting or the live `NewMessage` listener never receives supergroup messages (Fix 10, 2026-06-13, `14abd74`, `dialog-priming.ts`).** `catchUp()` is a literal no-op stub and there is NO `getDifference`/`getChannelDifference` recovery loop in `node_modules/telegram/client/updates.js` — the client only dispatches what Telegram pushes over the live socket, and Telegram does not push a channel's `UpdateNewChannelMessage` to an unprimed session. Symptom: AutoFetch (`client.getMessages`) works every sweep so the client/auth look healthy, but the live listener delivers nothing and DB ingest lag never drops below the sweep cadence (min 185.8s observed; live delivery is 1–5s). `primeChannelUpdates()` calls `getDialogs()` at startup (AFTER handlers are registered, so no first-update gap) and after every watchdog reconnect (a reconnect re-establishes the session and must re-prime). NEVER remove the priming call. The same `getDialogs()` result is the only way to check group MEMBERSHIP without opening a second live session (forbidden while prod runs): if the target group is absent from the dialog list, the account is banned/not a member (Milestone 5 `USER_BANNED_IN_CHANNEL`) and no code change can revive the live listener — swap the Telegram account/session string (explicit instruction required). **UPDATE (Fix 11, 2026-06-14): priming is NECESSARY-LOOKING BUT NOT SUFFICIENT.** Prod verification of deploy `a1c3958` proved priming SUCCEEDS and the account IS a member (`target group present in dialogs`, exact channelId 2129818880, on every startup + reconnect), yet the live listener STILL delivered nothing — so the ABSENT/ban branch is ruled out but live delivery remains broken. Do NOT treat a successful prime as "live listener works"; cross-check DB ingest lag. See the Fix 11 lesson below.
- **`/api/health.lastMessageReceivedAt` is initialised to BOOT time (server.ts:1320) and the watchdog never resets it** — so a freshly-deployed-but-dead live listener reads "healthy" (a recent timestamp) for up to 30 min after every deploy/reconnect. Never judge live-listener health from that field alone; cross-check DB ingest lag (`ingested_at - message_timestamp` on recent `messages`): batched lag ≥ ~180s means AutoFetch is carrying everything and the live listener is dead, sub-5s means it's genuinely delivering. **UPDATE (Fix 11, 2026-06-14): the watchdog now DOES reset `lastMessageReceivedAt` after a successful reconnect** (see Fix 11 below) — otherwise, with live delivery dead, silence stayed >30 min forever and it force-reconnected + re-primed every 5 minutes indefinitely.
- **Fix 10's getDialogs priming is necessary-looking but NOT sufficient; AutoFetch is the PRIMARY ingestion path (Fix 11, 2026-06-14, `autofetch-dedup.ts`).** Prod verification of deploy `a1c3958` (via `railway logs`): every startup + reconnect logs `Primed channel updates … target group present in dialogs` with an exact channel-id match (`matchedBy:"channelId"`, id 2129818880), so the account IS a member and the Milestone-5 ban does NOT extend to reading — the `TARGET GROUP IS ABSENT` / account-swap branch is RULED OUT. BUT live delivery is still dead: zero `Live message received` lines in 12h, min DB ingest lag 193.8s (newest 404.4s), all AutoFetch-batched, `lastMessageReceivedAt` frozen at boot. So GramJS 2.26.x does NOT push this supergroup's `UpdateNewChannelMessage` even when primed AND a member — the real cause is deeper (suspect low-level `client.connect()` vs `client.start()` / the update loop not re-arming), unverifiable on the no-telegram launcher, so it is PARKED as a research spike. Two changes shipped instead: (1) **watchdog reconnect-loop fixed** — the 5-min watchdog (30-min silence threshold) force-reconnected + re-primed EVERY 5 MIN forever because only live group messages advance `lastMessageReceivedAt` (Fix 6) and none arrive and the reconnect path never reset it; now `lastMessageReceivedAt = Date.now()` after a successful reconnect backs it off to ~30-min cadence (a genuinely dead socket still trips again in 30 min; `connectionRetries` covers real drops between). (2) **AutoFetch interval cut 15min→3min** (lag target ~60–180s) with a batched pre-dedup (`autofetch-dedup.ts`: `sweepCandidateIds` / `selectMessagesToIngest`, 15 unit tests) — ONE `messages.telegram_message_id` IN-query per sweep marks already-ingested ids so the sweep skips `checkIsAdmin` (a Telegram round-trip) + the 2.1s Groq-spacing sleep for them; the authoritative idempotent dedup STILL lives at the top of `processAndIngestMessage` (the pre-dedup is fail-open, never the sole guard). Don't widen `limit:20`/the 2h lookback without re-checking per-sweep cost.
- **Fix 11 VERIFIED IN PRODUCTION (2026-06-14, deploy `38f9d57`, via `railway logs` + live DB).** Healthy baseline to compare future regressions against: `Periodic check` sweeps land ~3 min apart (NOT 15); the watchdog `Force reconnecting` fires AT MOST ONCE per ~30-min silence window (boot+30min, then ~30-min cadence) with the counter reading `No Telegram messages received for 30 minutes` and `/api/health.lastMessageReceivedAt` jumping to the reconnect time — if you ever see `Force reconnecting` every 5 min or the counter climbing (500→530), the reconnect clock-reset regressed; post-deploy DB ingest lag (`ingested_at - message_timestamp`) sits ~37–174s, NOT 193–765s. **Still open & parked:** the live `NewMessage` push listener delivers nothing (`lastMessageReceivedAt` only advances on reconnect, never on a real group push), so AutoFetch is the SOLE ingestion path — the deeper GramJS 2.26.x push-stream spike (`client.start()` vs `connect()` / update-loop re-arming) is a separate task, ships straight to prod, needs explicit go.
- **Live-listener root cause PROVEN (2026-06-14 research spike, diagnostic commit `1f110a5`, `LISTENER_DEBUG` flag + `describeUpdate` in `telegram-guards.ts`): GramJS 2.26.x never tracks the channel's `pts` and never calls `updates.getChannelDifference`, so Telegram WITHHOLDS the supergroup's `UpdateNewChannelMessage` while still delivering the channel's lightweight CONTROL updates AND the account's whole common/DM update box.** The `connect()` vs `start()` / "update-loop re-arming" theory from the Fix 11 lesson above is DISPROVEN — leave it as a dead end. How it was cracked: an opt-in (`LISTENER_DEBUG=true`, default OFF, metadata-only — never logs message text) Raw handler logs every update's `className`/`channelId`/`pts`; armed on prod via `railway variables --set "LISTENER_DEBUG=true"`, captured with `railway logs`, correlated to the DB ingest-lag query, then disarmed. **`railway logs` in this CLI version STREAMS live lines only — capture it WITHOUT a pipe (let the background task file record stdout directly); `| tail`/`| grep` + `timeout` loses the buffered output and looks empty.** Hard evidence: captured `UpdateReadChannelInbox channelId:"2129818880" pts:215292` (a channel CONTROL update DOES arrive → not a ban, the channel IS in the push set) but test messages 139652/139653 posted 13:38:43Z produced ZERO `UpdateNewChannelMessage` and ingested only via the 13:41 AutoFetch sweep (~174s lag). Two separate pts spaces confirm it: channel ~215k vs user-box ~455k. **DISPROVEN, do not re-try these:** ChannelTooLong (NEVER seen — do NOT build a ChannelTooLong trigger), the Milestone-5 ban (channel control updates arrive fine), `connect()` vs `start()` (both wire `updateCallback: _handleUpdate` and start `_updateLoop`, TelegramClient.js:1070/1090/1103), and Fix 10's `getDialogs()` priming (lists the dialog but does NOT sync channel pts). The real fix (Phase 2, separate task, prod-only verifiable) is a `getChannelDifference` poll/trigger loop: seed + track the channel pts, call `updates.GetChannelDifference` on a short interval and/or on any channelId-2129818880 control update, feed `new_messages` into `processAndIngestMessage` (idempotent dedup keeps AutoFetch overlap safe), advance the tracked pts. Additive to AutoFetch (still the safety net); target sub-5s lag. NOT more priming.
- **Phase 2 — the `getChannelDifference` live-ingestion path is BUILT and VERIFIED IN PRODUCTION (2026-06-14, `channel-difference.ts`, `CHANNEL_DIFF_ENABLED=true` permanently on Railway, deploy `d9b11b3c`).** Seeds the channel pts via `Api.channels.GetFullChannel` → `fullChat.pts`, polls `Api.updates.GetChannelDifference({ channel, filter: ChannelMessagesFilterEmpty, pts, limit: 100 })` every 15s, feeds `newMessages` into the SAME idempotent `processAndIngestMessage`, re-seeds after every watchdog reconnect. **Three response shapes (pure module `classifyChannelDifference`, switch on `className`):** `updates.ChannelDifference` (new pts = `resp.pts`, has `newMessages`), `updates.ChannelDifferenceEmpty` (pts only), `updates.ChannelDifferenceTooLong` (**new pts = `resp.dialog.pts`, and its `messages` are LATEST-STATE not the gap — NEVER bulk-ingest; re-seed the pts and let AutoFetch's 2h lookback carry the gap**). Gotchas baked into `normalizeDiffMessage`: a raw TL `Api.Message`'s text is `.message` NOT `.text`; the sender comes from `fromId.userId`/`channelId` (a Peer, not a ready `senderId`); the @username must be joined from the response's `users[]` (raw messages carry only the numeric id); service/empty/media-only/no-id messages return null. **Idempotency relies entirely on the existing top-of-`processAndIngestMessage` dedup — do NOT add a second dedup in the diff path** (it is now the 4th overlapping ingestion path: live listener, AutoFetch, backfill, channel-diff). The diff path stamps `lastMessageReceivedAt` only on REAL delivery (keeps the watchdog asleep, like the NewMessage handler). **Live verification (2026-06-14):** Seeded pts:215301 at startup → posted test message "Alright" (telegram_id 139661, 17:28:35Z) → `Live channel message via getChannelDifference` at 17:28:51Z (count:1, newlyIngested:1, newPts:215302) → DB lag **14 seconds** vs AutoFetch baseline 60–177s; 1 messages row, no duplicate. KNOWN_ISSUES §6 item 1 is now FULLY CLOSED. AutoFetch (3-min) stays the safety net.
- **`getChannelDifference` TooLong shape carries no messages — it only advances pts. Never try to bulk-ingest history from it. Let AutoFetch carry the gap.**
- **Standalone scripts that use CommonJS (`require`) must be named `.cjs`, not `.js` — the project is `"type":"module"`, so Node runs every `.js` as ESM and a `require` throws `ReferenceError: require is not defined` (hit 2026-06-15 on `scripts/generate-session.js`). Renaming to `.cjs` is the smallest fix (no ESM rewrite needed). BUT `*.cjs` is gitignored (the "test and diagnostic scripts" rule, alongside `*.mjs`), so a REAL utility that must stay in the repo needs an explicit `!scripts/<name>.cjs` negation in `.gitignore` — otherwise the rename silently un-tracks it. Once the negation is in, `git` records it as a clean `.js`→`.cjs` rename. Run the session generator with `node -r dotenv/config scripts/generate-session.cjs`.**
- **Migration numbers in `supabase/migrations/` are a manual convention — `ls` the folder before naming a new one. The conversation-grouping plan said "migration 013", but `013_bot_replies` already existed (Milestone 5), so grouping shipped as `014_conversation_grouping_up/down.sql` (2026-06-15). The live application via the Supabase MCP `apply_migration` is tracked by NAME (`conversation_grouping`), independent of the repo's `0NN_` filename numbering — so keep the repo numbering collision-free yourself; the DB won't catch a clash.**
- **A `DELETE` inside a data-modifying CTE and a `count(*)` in the SAME statement both read the start-of-statement snapshot — so `WITH d AS (DELETE … RETURNING) SELECT (SELECT count(*) FROM t WHERE …) AS left` reports the PRE-delete count and looks like the delete failed (hit 2026-06-15 during test cleanup: `tickets_deleted:2` yet `tickets_left:2`). The CTE's `RETURNING` count is the authoritative "rows deleted"; always confirm a cleanup with a SEPARATE follow-up `SELECT` (which runs on a fresh snapshot).**
- **Railway rolling-deploy session-overlap permanently burns the GramJS session string (2026-06-16, commits `0bd0dbb` + `3a7da84`, `deploy-overlap.ts`).** Railway's rolling deploy: the NEW container starts, passes `/api/health` (Express boots ~instantly, before Telegram connects), then Railway tears down the OLD container. If the new container's `client.connect()` fires while the old one still holds the live MTProto session, Telegram responds `406: AUTH_KEY_DUPLICATED` and permanently invalidates the session string — every subsequent connect attempt also fails until you regenerate. Not transient; retrying in 10 minutes still fails. **Two-part fix — BOTH required:** (A) SIGTERM graceful-disconnect handler in `server.ts` (`process.once("SIGTERM"/"SIGINT")` → `client.disconnect()` raced against 3s timeout → `process.exit(0)`) so Railway's teardown SIGNAL fires BEFORE the new container's connect; (B) 60s startup connect delay via `TELEGRAM_CONNECT_DELAY_MS` env var (pure module `deploy-overlap.ts`, `resolveConnectDelayMs`, 12 unit tests) — the new container waits 60s before its first `client.connect()`, safely after the old container receives SIGTERM and disconnects. Default 60s; set `TELEGRAM_CONNECT_DELAY_MS=0` on a cold first deploy with no old container. The delay alone would fail if the old container is slow to disconnect; the SIGTERM handler alone would fail if Railway sends no SIGTERM (load-balancer timeout instead). Together they close the gap. Never redeploy with `numReplicas > 1` — each replica would burn the session immediately. **Post-fix verified baseline (2026-06-16):** logs show `Delaying initial connect 60s…` then `✅ Connected to Telegram (startup attempt 1)` with zero AUTH_KEY_DUPLICATED errors, pts seeded 215610, `telegramReady:true`.
- **GramJS session string has been rotated 3 times total (sessions #1 → #2 on 2026-06-15, #2 → #3 on 2026-06-16 due to rolling-deploy burn).** Each burn is permanent and requires running `node -r dotenv/config scripts/generate-session.cjs` with a valid Telegram account login, then updating `TELEGRAM_SESSION_STRING` in Railway and redeploying. Never regenerate without explicit user instruction. Session #3 (2026-06-16) is on a NEW personal Telegram account (normal group member). The rolling-deploy fix prevents future burns — `AUTH_KEY_DUPLICATED` on a lone container after the fix means the session string itself was externally invalidated (Telegram's side), not a deploy race.
- **A `human_skip` corrections row is a human-confirmed no-op (original = correct = current category), NOT a training signal.** When adding a new `correction_source` value, you must widen the live DB CHECK constraint via a migration (the reconstructed migration files do NOT reflect the live schema — the live constraint was `ANY(ARRAY['human_ui','admin_reply'])` and had to be ALTERed to add `human_skip`). Apply via Supabase MCP `apply_migration` (migration 015, tracked as `corrections_human_skip`, 2026-06-19). Also: every consumer of the `corrections` table that should exclude skips must add `.neq("correction_source","human_skip")` — currently `getFewShotCorrections` and `/api/verify`. The double-submit guard (`/api/train/correct`) must use `.in("correction_source", ["human_ui","human_skip"])` so a skip and a correct/wrong verdict are both treated as "already reviewed".
- **`railway logs` in this CLI version streams buffered lines only — a `| grep` pipe loses buffered output and shows nothing, making it look like the log stream is empty.** To wait for a deploy to go live, use an `until curl ... | grep -q "<new-commit-short-hash>"; do sleep 15; done` loop against `/api/health` instead. The `commit` field in `/api/health` is the authoritative indicator that the new container is serving.
- **A blank white page on iOS Safari that works in Telegram/Google in-app browsers is NOT a JS/CSS-compatibility bug — every iOS browser uses the same WebKit engine, so a compat fault would fail everywhere equally (Bug 1, 2026-06-19, `1e90ff4`).** When the symptom is intermittent and the page PAINTS after a repaint trigger (rotating the phone, tapping, opening devtools), it's a WebKit **compositing** failure, not a download/cache/code fault. Diagnostic ladder that cracked it: (1) `curl -sI` the prod HTML + assets — all `200` rules out server/404; (2) realize in-app-browser-works ⇒ not compat; (3) get the user to capture the on-device Network tab (all 200) + Console (no app error) with a Web Inspector Safari extension — proves JS ran and the DOM rendered; (4) the repaint-to-appear behaviour localizes it to compositing. Cause here: oversized decorative `blur-[120px]`/`blur-[150px]` blobs + a `backdrop-blur-xl` card on full-screen `min-h-screen` containers. Fix: shrink the blur radii and add `transform-gpu` (`translateZ(0)`) to the blurred elements + their containers so Safari GPU-composites on first paint. **This bug cannot be reproduced on desktop or this repo's preview-screenshot tool (it stalls on blur-heavy pages) — the user's iPhone is the only authoritative verification; say so honestly.** Ignore any `runtime.sendMessage() Tab not found` red banner during diagnosis — that's the Web Inspector extension's own error caught by the app's global `unhandledrejection` handler, not an app bug.
- **The Milestone-5 `USER_BANNED_IN_CHANNEL` block is the GROUP being broadcast-only, NOT an account-specific ban (re-tested on session #3, 2026-06-19).** Re-tested end-to-end: dry-run (`BOT_REPLIES_ENABLED=true`, `BOT_REPLIES_DRY_RUN` default on) produced a clean `dry_run` `bot_replies` row → the pipeline works on session #3; one controlled live send (`BOT_REPLIES_DRY_RUN=false`) failed identically with `400: USER_BANNED_IN_CHANNEL`. The user confirmed they cannot manually post to OfficialQuidaxCommunity either. So no session-string swap or code change fixes this — the account needs admin/posting rights granted by Quidax. Don't re-test send rights on future session rotations; the block is at the group level. Railway flags reset to `BOT_REPLIES_ENABLED=false` + `BOT_REPLIES_DRY_RUN=true` after the test; the test's failed row (`190ea97a…`) was deleted. **Railway auto-redeploys on every env-var change (~2.5 min incl. the 60s `TELEGRAM_CONNECT_DELAY_MS`), so an API call right after flipping a flag may still hit the OLD container — wait for the new container (watch `/api/health.lastMessageReceivedAt` jump to the fresh startup-connect time) before testing.**
- **A one-off `npm install <pkg>` intended for a throwaway subfolder can silently pollute the ROOT `package.json` + `package-lock.json` (hit 2026-06-19 while building the guide's `.docx` — `docx` was added to the app's real deps).** Even with a `cd subfolder; npm init -y; npm install` chain, npm can resolve up to the nearest existing `package.json` (the repo root). ALWAYS `git status` after any build-time install and `git restore package.json package-lock.json` before committing, and stage doc/build outputs by explicit path (never `git add -A`). Better: run such installs in a temp dir OUTSIDE the repo tree.
- **Doc generation on this Windows box: `pandoc` is NOT installed; build `.docx` with the `docx` npm lib via a small `md→docx` script in a temp folder (deleted after).** The docx skill's `scripts/office/validate.py` needs `pip install defusedxml` AND has a Windows cp1252 bug — it reads `word/*.xml` as cp1252 and crashes on UTF-8 bytes (the `•` bullet, smart quotes) AND can't print its own `→` to the console. Don't trust its "FAILED" output on Windows; validate structurally instead — `python` `zipfile.testzip()` + `xml.dom.minidom.parseString(z.read(part).decode('utf-8'))` on `[Content_Types].xml`/`word/document.xml`/`styles.xml`/`numbering.xml`. The Master Guide lives in `docs/guide/` (numbered `00`–`11`), assembled into `PULSEDESK_MASTER_GUIDE.md`, exported to `PULSEDESK_MASTER_GUIDE.docx`.
- **PowerShell here-string commit messages break on embedded double-quotes — `git commit -m @'…"quoted phrase"…'@` made git receive the words as separate pathspecs (`pathspec 'do' did not match`) and the commit failed (hit 2026-06-19).** Keep `git commit` here-string bodies free of inner `"` (rephrase without quotes), or the parse splits the message. Single-quoted here-strings still tripped on it in this CLI's PowerShell. (Robust alternative used since: write the message to a temp file OUTSIDE the repo and `git commit -F "$env:TEMP\msg.txt"`, deleting the temp files after — sidesteps all PowerShell quoting.)
- **KPI honesty fix Phases 1 & 2 (2026-06-19, migrations `016_kpi_honest` + `017_assumed_resolved`).** The resolution rate read a false-low 16% because the Active denominator was a graveyard. Phase 1: `tickets_stats` switched Avg→Median response (`percentile_cont(0.5)` — one 18-day outlier had dragged the mean to 8.4h vs true 6.5min) and now EXCLUDES the noise categories (General Question/Praise/Spam/Irrelevant) from `activeCount`; the `resolutionRate` JS at `server.ts` needed NO change because it reads `activeCount` straight from the RPC. Phase 2: new system-only status `Assumed Resolved` + dormant hourly sweep `assumeResolveQuietTickets` (pure module `assumed-resolved.ts`). Live results: 240→161 active, rate 16%→22% (Phase 1), projected ~38% once the Phase-2 backfill runs.
- **`tickets.last_message_at` is the authoritative per-ticket "quiet"/last-activity signal — `messages` has NO `ticket_id` FK, so you CANNOT join messages→tickets for last activity.** `last_message_at` is stamped with each message's OWN timestamp on every reply-attach path; use `coalesce(last_message_at, created_at)` for the rare null. The auto-resolve sweep keys on it.
- **"Admin-engaged" detection: use `raw_text LIKE '%[ADMIN_REPLY]%'` OR `first_admin_reply_at IS NOT NULL` — the column ALONE is sparse on legacy tickets (live: 20 vs 49 by raw_text).** `first_admin_reply_at` is only stamped on newer reply-attach paths, so it under-counts historical admin engagement; the raw_text `[ADMIN_REPLY]` block is the reliable signal for tickets that predate it.
- **The `Assumed Resolved` auto-resolve sweep is GATED behind `ASSUMED_RESOLVE_ENABLED` (default OFF) — it ships dormant** so the backlog can be previewed before any status is auto-changed. Eligibility = Open/In Review/Awaiting User, NEVER Escalated (`ASSUME_RESOLVABLE_STATUSES`); quiet threshold 7 days (`ASSUMED_RESOLVED_QUIET_DAYS`). The sweep uses a guarded conditional update (`.in("status", ASSUME_RESOLVABLE_STATUSES)`) and never posts to Telegram. A new USER message reopens Assumed Resolved (or Awaiting User) → In Review and clears `resolved_at` (the reopen lives in the user-reply paths at `server.ts`, NOT the admin paths). The rate counts `(resolvedCount + assumedResolvedCount)`; the conversation-grouping USER_FOLLOWUP path is irrelevant to reopen because its `last_message_at >= cutoff` (~5-min) window can never match a 7-day-quiet ticket.
- **Local-launcher auth: the x-admin-key for `/api/ingest` + `/api/tickets/:id/status` is `DASHBOARD_PASSWORD` (the actual `.env` var name — NOT `VITE_DASHBOARD_PASSWORD`; the code only copies VITE_→DASHBOARD_ when DASHBOARD_PASSWORD is missing).** Read it from `.env` with a raw-content regex (`Get-Content .env -Raw`; line-by-line `Where-Object` returned an array that broke `.Trim()`). The no-telegram launcher writes to the LIVE DB, so always clean up test tickets by `telegram_message_id` (cascades to the ticket).
- **KPI Phase 3 / conversation threading by active ticket (2026-06-19, `topic-shift.ts` + `groupingBand` in `conversation-grouping.ts`, NO migration).** The grouping branch in `processAndIngestMessage` folds a same-sender un-quoted message across a WIDE window, keyed on `gap = newMsgDate − candidate.last_message_at` (last_message_at advances on admin replies, so the gap means "time since ANY activity"): `fast` (≤`GROUPING_WINDOW_MS` 5 min → fold immediately, no LLM), `extended` (5 min–`GROUPING_ACTIVE_WINDOW_MS` 6 h → ONE Groq `llama-3.1-8b-instant` topic-shift call decides same-vs-different), `none` (>6 h or out-of-order → new ticket). Candidate = single most-recent ACTIVE ticket (Open/In Review/Escalated/Awaiting User) for the sender_hash; `Assumed Resolved` is deliberately NOT a candidate (the quoted-reply path already reopens it). `checkSameIssueViaGroq` reuses `groqBreaker`+`withTimeout`+`redactPII(sanitizeForPrompt())`; `parseTopicShiftDecision` is strict (`same_issue===true` only) and **every** failure path (parse/error/breaker-open/timeout) returns false → fall through to a NEW ticket, so it NEVER wrongly merges two issues and a Groq outage degrades to pre-Phase-3 behaviour. The topic-shift call IS awaited (its result gates the fold), adding ~1-2s to that minority of messages. **Crucially, the Phase-3 live leg WAS locally verifiable** even though the rule-of-thumb is "cross-network legs need prod" — grouping never calls `tlClient` (only the live `NewMessage`/`getMessages` paths do) and the launcher's Groq is real, so `/api/ingest` exercised the full path incl. the topic-shift LLM. Verified all 4 bands + idempotency live (fast fold logs NO topic-shift line; extended logs `SAME issue (fold)` / `DIFFERENT issue (new ticket)`; re-ingest → `ticket:null`, still exactly one `[USER_FOLLOWUP]`). The `telegram_message_id` dedup at the top of `processAndIngestMessage` was NOT touched. `/api/ingest` `senderId` scopes the sender_hash, so a unique test senderId isolates the test from real tickets (grouping is sender-hash-scoped) — backdating msgDate is then belt-and-suspenders, not strictly required for grouping.
- **Phase 4 / noise pre-filter (2026-06-20, `noise-prefilter.ts`, commit `fad4fdcb`).** `isBanterNoise(text)` is OR'd into the `isPreFiltered` gate: `isPreFiltered = !skipPreFilter && (!shouldProcessMessage(...) || isBanterNoise(text))`. Two patterns: **1a** bare price-bot commands (whole trimmed text must match `^\s*\/(?:p|price|c|chart)\s+[a-z0-9]{2,10}\s*$` — a question or sentence AFTER the command still flows through); **1b** long news pastes (≥25 words AND no `?` AND no first-person pronoun AND no Quidax/account/support keyword AND starts with proper-noun + reporting verb or journalism prefix). Both patterns are conservative AND-gates that bias heavily toward FALSE (a false-negative goes to Groq for real classification; a false-positive hides a real issue — so when in doubt `isBanterNoise` returns false). Pattern 1c (user-to-user chatter) was evaluated and DEFERRED — the signal-to-noise ratio was too low at current volume. `isPreFiltered=true` skips the LLM call and assigns `category:"General Question" / status:"Dismissed"` (reversible in /train); the `messages` row is always persisted (dedup unchanged). The `skipPreFilter` flag in `/api/ingest` and `/api/backfill` means admin-simulated messages are never caught by the pre-filter.
- **Manual-audit fix: quoted-reply silent drop (2026-06-20, `cc07f58`).** The non-admin quoted-reply branch in `processAndIngestMessage` used to `return null` when the quoted parent matched no active ticket — so a REAL user issue arriving as a reply to an old/welcome message created NO ticket, and the admin's later replies to it dropped as unattached-admin → the whole live conversation was invisible on the dashboard (this was the true cause of "I'm not seeing today's messages", NOT an ingestion failure — every message was in `messages` with 8–15s lag). Fix: delete the `return null`, fall through to the existing grouping + new-ticket-creation logic. Safe because the top-of-function `telegram_message_id` dedup keeps re-scans idempotent across all 4 ingestion paths, and banter (replies to price/news/welcome) still gets Dismissed by the pre-filter/classifier. Deliberately do NOT recover the quoted parent here (unlike the admin Fix-5 path) — a user replying to the welcome bot shouldn't spawn a welcome-message ticket; the user's own message carries the issue. The feed was ALSO re-sorted from `created_at` to `last_message_at desc` (`b08f106`) so freshly-replied old tickets surface — the two together fixed the "today looks empty" symptom.
- **Phase D2 / conversation-aware resolution inference (2026-06-20, `conversation-resolution.ts`, commits `21a051a` + `ad2efda`, LIVE on Railway `RESOLUTION_INFER_ENABLED=true` + `RESOLUTION_INFER_DRY_RUN=false`).** The 7-day `assumeResolveQuietTickets` time-sweep is blunt: every admin reply bumps `last_message_at` and resets the quiet clock, so an actively-worked-then-solved ticket sits "In Review" for a week. D2's `inferResolvedFromConversation()` sweep (startup+4min, then hourly — staggered after the 7-day sweep's +3min) reads the WHOLE thread (raw_text IS the full thread: original + labeled `[ADMIN_REPLY]`/`[USER_FOLLOWUP]` blocks) of an admin-engaged, active, NON-Escalated ticket quiet ≥`RESOLUTION_INFER_QUIET_HOURS` (default 24h) and asks Groq (`llama-3.1-8b-instant`, temp 0, `groqBreaker`+`withTimeout`+`redactPII(sanitizeForPrompt())`) "did support resolve the issue?". Pure module owns `buildResolutionMessages` + strict `parseResolutionDecision` (only literal `resolved===true`; EVERY parse/error/breaker/timeout path → false, ticket untouched). Write = the SAME guarded conditional `.in("status", ASSUME_RESOLVABLE_STATUSES)` update as the time sweep → never clobbers a concurrent human change, never touches Escalated, stamps `resolved_at`, never posts to Telegram. Bounded at 40 Groq calls/sweep, spaced 2.1s. Two fail-safe flags mirror the bot rails: `RESOLUTION_INFER_ENABLED` (default OFF) + `RESOLUTION_INFER_DRY_RUN` (default ON → logs `[DRY] would infer-resolve…` and writes nothing). **The dry-run preview is essential and caught a real precision miss** — the first prompt closed a ticket where the admin was awaiting a screenshot; fix was prompt-only (focus on the LAST support message + "is anything pending on the user?" + 4 worked examples, same worked-example pattern as the pidgin glossary), NOT a model swap (the whole codebase uses `llama-3.1-8b-instant`; an unverified bigger model name would silently 404→fail-safe-false). Live result: rate 37.0%→38.4%, exactly the 2 correct tickets flipped, 0 Escalated touched.
- **Railway CLI gotcha (2026-06-20): `railway variables --set "KEY=val"` updates the variable but did NOT auto-redeploy in this CLI version** — a running container keeps its boot-time env, so the change only takes effect on the NEXT deploy. Force it with `railway redeploy --yes`. (This contradicts an older note that Railway auto-redeploys on every env change — that may apply to dashboard edits, not the CLI `--set`.) Detect the new container via `/api/health.lastMessageReceivedAt` jumping to a fresh boot time (commit stays the same on an env-only redeploy, so `commit` is NOT a usable signal there). A timed sweep (setTimeout at boot+Nmin) fires relative to module load, so wait boot+Nmin then verify against the LIVE DB — `railway logs` only keeps the current container's recent buffer and can miss a sweep line by seconds.
- **Changing a reply-block tag (`[ADMIN_REPLY]`/`[USER_REPLY]`/`[USER_FOLLOWUP]`) is NOT a local edit — it ripples into multiple EXACT-MATCH parsers (Phase 1 lost-message rebuild, 2026-06-20).** The plan called for tagging appended blocks with `id=<telegramId>`; that was DROPPED because the suffix breaks: (1) `conversation-grouping.ts`'s `BLOCK_RE` (`:36`) and `FIRST_BLOCK_RE` (`:41`) require a bare `]` right after the tag name, so `[USER_FOLLOWUP id=123]` stops matching → grouped-ticket reclassification silently loses the follow-up text; (2) the admin-engaged detection in `assumeResolveQuietTickets` and `inferResolvedFromConversation` uses literal `t.raw_text.includes("[ADMIN_REPLY]")` (`server.ts:1148`/`:1261`) → an `id=`-tagged admin block would no longer count as admin-engaged, freezing those sweeps. Before tagging or renaming any block, grep ALL consumers: the App.tsx thread parser + `handoff-detect.ts` already tolerate suffixes (`[^\]]*`), but the two above do NOT. The era-agnostic alternative that needs no tag: detect "already attached" by a raw_text substring search (`tickets.raw_text ILIKE '%<message text>%'`), which catches every attach path past and future.
- **Reconciliation / any `messages`-driven sweep CANNOT use `checkIsAdmin` — `messages` stores only `sender_hash`, never `senderId` (Phase 1, 2026-06-20, `message-reconciliation.ts` + `reconcileOrphanMessages()`).** A sweep replaying orphaned messages can't re-run the live admin check, and admin/bot messages are DROPPED (never ticketed) so their hashes are absent from the `tickets WHERE is_admin_message=true` set you'd derive admin hashes from. The welcome/ban bots therefore slip through as "orphans" AND are long enough to pass `shouldProcessMessage`, so without a gate they come back as Open tickets. Fix pattern: a content recognizer `isSystemBotMessage(text)` (welcome/ban TEMPLATES — unmistakable, err toward NOT matching) PLUS reusing the live noise gate (`shouldProcessMessage`/`isBanterNoise`, incl. the new price-bot pattern 1d) so only REAL actionable user messages are resurrected. **Dry-run such a sweep against the live DB before enabling — the read-only preview (`scripts/reconcile-preview.mjs`) is what surfaced this whole class of false-orphan before a single write.** The reconcile sweep ships dormant (`INGEST_RECONCILE_ENABLED` default OFF + `INGEST_RECONCILE_DRY_RUN` default ON); it replays orphans through the SAME `processAndIngestMessage` via a 3-line `reconcileOpts` bypass (overrides sender_hash to keep a multi-msg convo grouped, reuses the existing `messages.id`, skips the dedup+insert); the top-of-function `telegram_message_id` dedup is the authoritative idempotency guard (re-runs converge to 0 orphans). It's locally verifiable on the no-telegram launcher (never calls `tlClient`); trigger it fast with `INGEST_RECONCILE_STARTUP_DELAY_MS`.
- **NEVER spin up a second GramJS session to "re-fetch" historical Telegram messages while prod is live — it burns the session string (`AUTH_KEY_DUPLICATED`, the burn that has already happened twice).** When asked to recover dropped Telegram messages (e.g. admin replies dropped before they became visible), restore from the `messages` table, which already holds the genuine ingested `raw_text` — and source the content FROM `messages` in the SQL (a `WITH ... SELECT raw_text FROM messages WHERE telegram_message_id IN (...)` data-relocation), do NOT hand-type the text inline (the auto-mode classifier correctly blocks inline-typed admin text as fabricated/impersonation; the messages-sourced relocation is provably genuine and is allowed). The reply-to link that was never stored is unrecoverable from the DB — state that limitation honestly rather than reconstructing it from a risky live re-fetch.
- **Accuracy-audit credibility stack (2026-06-21, `audit-scoring.ts` + `scripts/audit-*`).** Self-authored gold cases fail "you wrote the test AND the answer key"; the `/api/verify` panel is selection-biased (it only sees the AI's hardest, human-corrected cases). The defensible number = a **real-traffic agreement audit**: stratified sample of real `tickets`/`messages`, classified by the **DEPLOYED `/api/eval`** (NOT a re-implemented prompt — avoids drift, and `/api/eval` does NO DB writes, so it's read-only-safe), then judged BLIND by a human (the AI key is withheld in `audit/agreement_key.json`). Grading rules that make it fair: category exact-match + a confusion matrix; urgency **"within one level"** (Low<Medium<High<Critical) AND **recall on Critical/High** (the business metric — catching the urgent ones); blank/invalid human cells are EXCLUDED from every denominator (so a rater typo can't fake the number, and a rater can confidence-gate by leaving unsure rows blank). Architecture: a committed, unit-tested PURE core `audit-scoring.ts` (the gameable-proof piece) + committed scorers `scripts/audit-score*.ts` + the operational samplers `scripts/audit-sample.mjs`/`audit-system.mjs` (negated back into git so the method is reproducible) → numbers-only `audit-results.json` (NO message text/PII); real worksheets live in the gitignored `audit/` dir. **System-level trust metrics matter MORE than classification %** (the scary failures): auto-resolve precision (audit ALL Assumed-Resolved, split D2-vs-7-day via `resolved_at - last_message_at < 7d`), noise false-negative rate (audit ALL actionable-category Dismissed + a banter sample), grouping over-merge/over-split (audit all folds + same-sender clusters).
- **Vite/Rollup CONSTANT-FOLDS a static JSON import (2026-06-21).** `import auditResults from "../audit-results.json"` with `agreement: null` makes Rollup **dead-code-eliminate** the populated branch of `A.agreement ? (full card) : (pending state)` — only the branch matching the committed JSON ships. So the dashboard's "Real-Traffic Accuracy Audit" card is **build-time-baked**, not runtime-dynamic: ALWAYS `npm run build` after updating `audit-results.json`, or the card stays on whatever the committed JSON selects. Needs `resolveJsonModule: true` in tsconfig for the `tsc` gate (added 2026-06-21). Verify a reframe shipped by grepping the built `dist/assets/index-*.js` for the new strings (a span-split string like "not</span> overall accuracy" won't match contiguously — that's expected, not a miss).
- **xlsx-with-dropdowns via temp-dir exceljs (2026-06-21).** Build dropdown-validated rating worksheets with `exceljs` installed in a temp dir OUTSIDE the repo tree (the root-`package.json`-pollution gotcha — `npm install` resolves up to the nearest manifest), reading a JSON the sampler wrote, writing the `.xlsx` back into the repo's `audit/`; ALWAYS `git status --porcelain package.json package-lock.json` after (must be clean). CSV is the zero-dep fallback — the scorers parse CSV, so the rater can also just export the filled sheet to CSV.
- **The real-traffic audit surfaced two PRODUCT signals, not just numbers (2026-06-22).** (1) Context-free one-off community messages (the fragmentation artefacts like "USDT too", "Order book also") are **unjudgeable even by an expert** without the surrounding thread — which IS the product thesis: PulseDesk's job is to assemble conversation context so a snippet becomes a real ticket. (2) The taxonomy lacks a **"Community Chat"** bucket: greetings / user-to-user chatter currently route to **Spam/Irrelevant** by `GROQ_SYSTEM_PROMPT` design (`server.ts` ~1601: "greetings, off-topic, emojis only, price discussion"), conflating scams with benign chatter. A dedicated category routed to the noise lane is the clean fix; noise-prefilter "pattern 1c" was the deferred precursor. For a fair audit, the human must rate by the SAME taxonomy the AI was given (greetings → Spam/Irrelevant), flagging the gap in Notes rather than inventing a category.
- **Admin replies were being DROPPED by an attachment ASYMMETRY (Bulletproof-ingestion Phase A, 2026-06-22, `admin-reply-attach.ts`).** Users got WIDE attachment (grouping 6h + quoted-fallback 48h + sender-hash), but admins only attached via a quoted reply to a ticket ROOT or a hard 90s `created_at` window — so an admin answering minutes later fell through `disposeUnattachedMessage → drop-admin` and the ticket sat Open with `first_admin_reply_at=null` and zero `[ADMIN_REPLY]` (proven on `5aec106f`: 4 real admin replies in `messages`, never attached). Fix = symmetry: (A2) a quoted admin reply whose quoted message maps to no ticket now resolves via that quoted **message's `sender_hash`** to the sender's most-recent active non-admin ticket (catches "admin quoted a folded follow-up"); (A3) the un-quoted window widened 90s→~30min (`ADMIN_UNQUOTED_ATTACH_WINDOW_MS`, env `ADMIN_REPLY_ATTACH_WINDOW_MS`), single most-recently-active candidate via the pure `selectAdminAttachTarget`. `disposeUnattachedMessage` STAYS the final fallback so genuine admin news/banter with no active-ticket match still correctly drops (never a standalone ticket). The dropped replies also crater the resolution rate: the 7-day + D2 auto-resolve sweeps gate on `adminEngaged = [ADMIN_REPLY] in raw_text OR first_admin_reply_at`, so an unattached reply makes a solved ticket look unengaged and sit Open forever.
- **Edited/deleted Telegram messages were NEVER handled in production because the edit/delete logic rode the DEAD live Raw listener (Bulletproof-ingestion Phase B, 2026-06-22, `channel-difference.ts` `extractChannelEditsDeletes`).** `UpdateEditChannelMessage`/`UpdateDeleteChannelMessages` ride a channel difference's `resp.otherUpdates`, which the `getChannelDifference` drain was DISCARDING (it processed `newMessages` only). Fix: surface `otherUpdates` on the `kind:"messages"` classification + a pure `extractChannelEditsDeletes` that pulls `{edits:[{id,text}], deletedIds:[]}`, applied through shared `applyMessageEdit`/`applyMessageDelete` helpers the dead Raw handler ALSO still calls (no behavior fork). Delete = soft-delete (`messages.deleted_at`, migration `018_messages_deleted_at`) + a GUARDED root-Dismiss `.in("status",["Open","In Review","Awaiting User","Assumed Resolved"])` (never clobbers Escalated/Resolved); a deleted follow-up stamps `deleted_at` only and leaves the parent (issue may still be live). GOTCHA: a raw TL `Api.Message`'s text is `.message` NOT `.text`, and `Number(null)===0`/`Number("")===0` — so the id guard must be `Number.isFinite(idN) && idN > 0` (real Telegram ids are positive). **The live edit/delete-over-`otherUpdates` leg is PROD-ONLY verifiable (no `tlClient` locally) — its first real exercise is the deploy; say so honestly, like the rest of the channel-diff path.**
- **Time-based historical backfill of dropped admin replies CANNOT separate support replies from market/news chatter when the support account posts both (Bulletproof-ingestion Phase C, 2026-06-22).** Preview-first caught it: a broad `[created_at, last_message_at]` window pulled "GM Quidax Fam"/"BREAKING: Fed"/"dips are always for the taking" into tickets. Tightening (30-min window + 36h-span exclusion + content guards) still left pollution on 5 multi-day tickets (`e4336ad0`,`0c72808f`,`7c36d5f4`,`c1f3954b`,`1ccffe2c`); the honest move was to EXCLUDE them and backfill only a hand-verified-clean subset (50 replies onto 19 tickets, text sourced FROM `messages`, never hand-typed). Reliable attribution of the excluded 5 needs the deferred Phase-2 reducer keyed on **reply-to metadata** (`messages` does NOT store `replyToMsgId` today). Honest rate lift was ≈+2pts (38.9%→~40.9%), not the loose ~50% first estimate — accuracy over rate, every time.
- **The Phase-C time-window backfill mis-attributed admin replies onto the WRONG ticket — root cause is the single missing signal `reply-to` (Reply-To-Attribution plan, 2026-06-22, migration `019`).** A user audit proved it on the withdrawal ticket `bad126a1`: it showed 5 `[ADMIN_REPLY]` blocks but only the first was real — the other four were the admin answering OTHER people (Qahir's "Hello", Phoenix's scam question) and got pulled in by clock-time. Confirmed systemic: ~25 distinct admin messages double-attached across tickets (e.g. "They are in on it" 140053 on both Phoenix's ticket and `bad126a1`). The SAME gap over-splits: a user answering an admin's question (139904 quoting the admin) spawned a NEW ticket instead of continuing the login ticket, because `messages` has **no `ticket_id` link** so the quoted admin message couldn't map to its ticket. Telegram gives `replyToMsgId` on every reply (channel-diff `normalizeDiffMessage`, AutoFetch, live handler all already compute it) but it was NEVER persisted. **The durable fix (Phase 2, pending): add `messages.reply_to_msg_id` + `messages.ticket_id` (migration 019), set ticket_id on EVERY attach via a `linkMessageToTicket` helper, and make both quoted branches reply-to-FIRST — load the quoted message's `ticket_id` and attach there as ground truth before any time/sender fallback.** Historical reply-to is UNRECOVERABLE (never stored; re-fetch forbidden — burns session), so historical cleanup is preview-first + user-reviewed only, never a heuristic re-derivation.
- **Cleanup technique for merging same-user split tickets (Phase 1, 2026-06-22): rebuild the survivor's `raw_text` by sourcing block text FROM the `messages` table via SQL string-concat, never hand-typed** (data-relocation is allowed and provably genuine; hand-typed admin text is blocked as impersonation). Survivor = earliest-created; order blocks chronologically by telegram_message_id (a plain `f.raw_text || e.raw_text` concat gets the order wrong when the two tickets interleave). **Before deleting the merged-away ticket: re-point its `corrections` rows (`UPDATE corrections SET ticket_id=<survivor>`) — corrections cascade-delete with the ticket and a training signal is lost otherwise** (verify the global corrections count is unchanged after). `messages.ticket_id` has NO FK to tickets, so a ticket delete does NOT cascade messages — re-point them explicitly (`UPDATE messages SET ticket_id=<survivor> WHERE ticket_id=<deleted>`) for future-reply correctness. Confirm deletes with a SEPARATE follow-up SELECT (the same-statement-snapshot trap). The pre-delete double-attach detector (admin text on 2+ tickets) has false positives for GENERIC phrases ("You are welcome", "Send an email to support") that legitimately repeat as separate messages — only DISTINCTIVE multi-ticket texts are real pollution; don't auto-remove generic matches.
- **Resolution-rate diagnosis (2026-06-22): only 14 of 116 active non-noise tickets showed admin engagement though admins reply to nearly all — because the replies were dropped/mis-attributed.** That ratio (not the rate %) is the real story: fixing attachment (Phase 2) is the biggest rate lever (correctly-attached replies → the auto-resolve sweeps engage → rate climbs). The other drag is email handoffs: the most common admin reply is "send an email to support@quidax.com" (a handoff, not an in-channel resolution PulseDesk can observe) — Phase 3 adds a `Handed off` status excluded from the active denominator. Post-Phase-1 rate was 43.7%.
- **Reply-to ground-truth attribution Phase 2 SHIPPED & DEPLOYED (2026-06-22, commits `553b516` migration + `3191cd5` code, prod healthy, Telegram reconnected clean).** Every `messages` row now persists `reply_to_msg_id` (pure `normalizeReplyToMsgId` — the `> 0` guard is load-bearing: `Number(null)===0`/`Number("")===0` would store 0 for a non-reply) and gets `ticket_id` stamped at ALL SIX attach sites via the best-effort `linkMessageToTicket` helper (root insert, user auto-resolve, admin quoted+unquoted append, user reply append, grouping fold) — a link failure logs a warn and NEVER aborts ingestion (message+ticket already exist). Both quoted branches are reply-to-FIRST: the `parentMsg` select includes `ticket_id`, and pure `selectReplyToTarget` (in `reply-target.ts`; gate = non-null, non-admin ticket, status ∈ `REPLY_ATTACH_STATUSES` = the 4 active + Assumed Resolved; rejects Resolved/Dismissed) decides ground truth BEFORE the message_id/sender-hash/ilike/time fallbacks. Unanswered-burst fast-fold = pure `shouldBurstFold` (conversation-grouping.ts) + `GROUPING_BURST_WINDOW_MS` (default 30 min): in the EXTENDED band, an `Open` candidate with `first_admin_reply_at IS NULL` folds WITHOUT the topic-shift Groq call. **GOTCHA when e2e-testing the quoted-USER ground-truth branch: a reply whose text contains thanks/thank you/resolved/fixed/worked/solved/appreciate hits the `isResolution` auto-resolve branch FIRST (it runs before the `replyToMsgId` branches) — use neutral reply text or you'll exercise the wrong path.** The live channel-diff/AutoFetch/live handlers already COMPUTED `replyToMsgId` pre-change; Phase 2 only persists it in the shared `processAndIngestMessage`, so the only prod-only-unverified surface is "a real quoted reply storing the column over the socket" — the attach logic itself is fully e2e-proven via `/api/ingest`.

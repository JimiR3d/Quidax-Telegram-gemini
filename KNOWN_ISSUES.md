# PulseDesk: Known Issues & Pending Features

This document provides a brutally honest, exhaustive tracking of every bug, suspected bug, untested area, repeated fix, and pending feature in the PulseDesk application.

## 1. Repeated Fixes & Chronic Bugs
*   **LLM Schema Hallucinations:** The Groq/LLaMA integration has repeatedly returned JSON payloads with incorrect keys (e.g., `priority` instead of `urgency`) or varying casing. This was patched multiple times: first with stricter system prompts, then with Zod validation, and finally with a custom `normalizeCategory` fallback function. It remains an area of fragility if the model updates or shifts behavior.
*   **Supabase Row Level Security (RLS) Conflicts:** Initially, the backend attempted to insert records using the `SUPABASE_ANON_KEY`, which failed because RLS policies blocked anonymous inserts. This was fixed by exclusively using `SUPABASE_SERVICE_ROLE_KEY` on the backend, but any future frontend direct-database writes will instantly hit this wall if not properly scoped.
*   **AI Context Degradation:** Development environments (like AI agents) repeatedly lost the context of the project or hallucinated previous fixes because temporary build artifacts and test scripts polluted the context window. This required aggressive `.gitignore` management and manual context handoffs.

## 2. BUGS (confirmed)
*   **~~Admin reply duplication~~ — FIXED (2026-06-11):** Admin replies appeared 2-3x (up to 23x in the worst ticket) in the dashboard stream. Root cause: the duplicate-message check inside `processAndIngestMessage` ran *after* the reply-handling branches, so the AutoFetch periodic re-scan (every 15 min, 2-hour window) and manual backfills re-appended `[ADMIN_REPLY]`/`[USER_REPLY]` blocks to tickets on every pass. Fix (commit `3b04b54`): moved the `telegram_message_id` dedup check to the top of the function before any branch, and user replies are now recorded in the `messages` table so re-processing catches them too. Existing data was cleaned: 446 duplicate blocks removed across 82 tickets via a previewed-then-applied script; a re-scan confirmed zero duplicates remain.
*   **~~KPI cards unverified~~ — AUDITED & FIXED (2026-06-11, commit `2ea6bea`):** Full audit against ground-truth DB counts found: (a) "Resolved" metrics counted 481 Dismissed spam tickets as resolutions, inflating Resolution Rate from a true 53% to 80%; (b) "Resolved Today" was meaningless — it read `updated_at`, which no code ever maintained (797 of 822 tickets shared a one-off backfill date of 2026-06-09), while the dedicated `resolved_at` column was populated on 0 tickets; (c) "today" used the server's timezone (UTC on Railway) instead of Lagos. Fixed by product decision (Option A): Resolution Rate = Resolved ÷ (Resolved + Active) with Dismissed excluded everywhere; all four status-writing code paths now stamp `resolved_at`/`updated_at`; "today" computed in Africa/Lagos. Verified live: rate 80%→53%, Resolved All Time 661→180, and a resolve/reopen round-trip correctly ticked Resolved Today 0→1→0. Note: legacy tickets resolved before this fix have `resolved_at = null` and are intentionally never counted in "Resolved Today" (no fabricated timestamps).
*   **~~Avg Response Time KPI does not exist~~ — FIXED (2026-06-12, Milestone 2):** A `first_admin_reply_at` column was added (migration 009, applied to the live DB with explicit confirmation). Both admin-reply attachment paths (quoted reply and the 90-second heuristic) stamp it once, using the reply message's own timestamp so backfills stay accurate, and never move it on later replies. The dashboard now shows an "Avg Response Time" KPI card = mean of (first admin reply − ticket creation) over tickets that have a tracked reply, respecting all base filters. Legacy tickets keep `first_admin_reply_at = null` and are excluded — same no-fabricated-timestamps precedent as `resolved_at`. Verified end-to-end 2026-06-12 (measured 23s on a simulated ticket, `avgResponseMs` confirmed in the stats payload, test data cleaned up).
*   **~~Filter inconsistency~~ — AUDITED & FIXED (2026-06-11, commit `9c2ea4f`):** The audit proved category/urgency/date filters already updated the KPI cards. The real bugs: (a) the search box filtered the table but not the KPI cards (103 table rows vs 822 stats rows, live evidence); (b) custom date ranges silently dropped the whole end day ("today→today" returned 0 while 4 tickets existed today); (c) "last N days" cut at server-clock midnight (UTC on Railway) instead of Lagos. Fixed: search now lives in the shared base filters; all date boundaries are Lagos calendar days. Verified live against a server running TZ=UTC. The `status` filter intentionally stays table-only (KPI cards break down by status).

## 3. ARCHITECTURE GAPS & Suspected Bugs
*   **~~Stats query 5,000-row cap~~ — FIXED (2026-06-12, Milestone 4):** KPI aggregation moved into Postgres (`tickets_stats` function, migration 012) over the full filtered set; `/api/tickets` calls it via RPC and no longer ships raw rows (`rawStatsData` removed — the volume chart now uses the function's `volumeByDay` series, bucketed by Lagos day). Parity with the old in-memory numbers proven across 7 live filter combinations before the old code was deleted.
*   **~~Resolution detection is unreliable for unquoted admin replies~~ — MITIGATED (2026-06-11, commit `6c66bf2`):** A 90-second window heuristic now attaches an unquoted admin message to the most recently ingested Open/In Review ticket in the same group (marking it "In Review") when that ticket arrived within the previous 90 seconds. Remaining limitation, accepted by design: if several users post within the same 90 seconds, the reply attaches to the newest ticket, which may be the wrong one. (The second limitation this entry used to carry — the async classifier overwriting "In Review" — was FIXED 2026-06-12, see below.)
*   **~~Async classification overwrites statuses set during the ~5-10s window~~ — FIXED (2026-06-12, Milestone 4):** the background classifier's final update is guarded: it writes status only if the status is still exactly what ingestion inserted (atomic conditional update); if an admin reply, user reply, or dashboard action changed the status mid-classification, the classifier saves category/summary/urgency/reply WITHOUT touching status. All three write sites covered (Groq, Gemini fallback, failure path). Proven with a live race test: "Escalated" set during classification survived, classification fields still landed.
*   **~~Binary Resolution Status~~ — FIXED (2026-06-12, Milestone 2):** Tickets now support a 4-state active workflow: Open / In Review / Escalated / Awaiting User (plus Resolved and Dismissed). The DB CHECK constraint was widened (migration 009). Rules: an admin reply moves Open → In Review but never un-escalates (Escalated and Awaiting User keep their state); a user reply to an Awaiting User ticket flips it back to In Review; all four active states accept incoming replies and count as "Active" in the resolution rate denominator. The "In Review" KPI card now shows the true In Review count (it previously mislabeled it `escalatedCount`) with an escalated/awaiting-user breakdown beneath it. Verified end-to-end 2026-06-12 via `/api/ingest` simulations; invalid statuses still rejected with 400.
*   **~~Nigerian Pidgin English~~ — FIXED (2026-06-13, Fix 9, commit `0354351`):** Common Pidgin phrases like "e don do", "e never enter", "dem block my account", "abeg help me", "una too much" were misclassified or defaulted to General Question. Fix: `pidgin-glossary.ts` (pure module, 7 unit tests) exports `PIDGIN_GLOSSARY_PROMPT` — a phrase-mapping glossary + 5 worked examples — which is appended to `GROQ_SYSTEM_PROMPT` in server.ts. This flows to the main pipeline, Gemini fallback, /api/test-message, AND /api/eval (still a raw-model baseline; glossary is part of the base prompt, not few-shot). Benchmark extended to 18 cases (+ 6 Pidgin, ids 13-18, different wording from the glossary examples so /api/eval measures generalisation). Proven locally: 6 Pidgin cases 83%/83%/67% before → 100%/100%/100% after; full 18-case run: category 100%, urgency 94%, overall 94% (pre-existing English urgency miss on case 9 unchanged). 71/71 tests green. **LIVE-VERIFIED in production (2026-06-13, `22d71ba` deploy):** `GET /api/eval` returned `total:18`, category 100% / urgency 94% / overall 94% — the 6 Pidgin cases (ids 13–18) all passed BOTH category and urgency (6/6 / 6/6); the sole miss is the pre-existing English case-9 urgency (Trading Problem Medium→High), not Pidgin. Fix 9 confirmed working against the raw-model baseline on live infrastructure.
*   **Keyword Learning System:** The `learned_keywords` system exists but its long-term effectiveness has not been measured or verified in a live, prolonged production environment to prove it actually improves accuracy.
*   **GramJS Session Expiry:** The `TELEGRAM_SESSION_STRING` is currently treated as permanent. It has never been tested against Telegram's forced session revocations or expiry intervals. If it expires, the listener will silently die without auto-recovery.
*   **Extreme High-Volume Concurrency:** The async ingestion pipeline and heavy limiters work for standard volume. It is highly suspected that a massive raid or spam attack (e.g., 5,000 messages in 10 seconds) could overwhelm the Groq rate limits, causing the backend to start dropping messages despite the circuit breaker.
*   **Cloud Run / Railway Deployment:** GramJS requires a persistent TCP connection (WebSocket), which traditionally conflicts with serverless environments like Vercel. While containerization (Cloud Run / Railway) works in theory, long-running persistent connections often get severed by load balancers after 30-60 minutes unless active keep-alives are meticulously tuned. This tuning is currently unverified.
*   **~~Gemini 503 "high demand" errors leave tickets without a suggested reply~~ — FIXED (2026-06-12, Milestone 4):** `generateSuggestedReply` now retries capacity errors (429/5xx, "high demand", timeouts) up to 3 times with exponential backoff + jitter — each attempt is a separate circuit-breaker call, and a breaker-open fast-fail is never retried. Backstop for outages that outlive the retries: a sweep every 15 minutes regenerates missing replies on classified ACTIVE tickets from the last 24h (max 10 per run; never overwrites a reply that appeared in the meantime). Verified live end-to-end; the 661 historical null-reply tickets (resolved/dismissed/old) are deliberately out of scope. "Missing suggested reply" is now a ≤15-minute condition, not a permanent one. **Updated by Fix 8 (2026-06-13, §6 item 7):** the retry path now distinguishes a *transient* 503/overload (still retried with short backoff) from a *quota* 429/RESOURCE_EXHAUSTED, which arms a 60-min cooldown instead of being retried — otherwise the daily-quota case made the sweep re-burn the quota and trip the breaker every cycle.

## 4. PENDING FEATURES (not yet built)
*   **~~Human feedback training interface~~ — BUILT (2026-06-12, Milestone 3):** /train route shows unreviewed tickets newest-first, one at a time, with "Correct" / "Wrong — fix it" buttons and a category dropdown. Corrections stored immediately in the `corrections` table; "wrong" also updates the ticket and writes an audit log entry.
*   **~~Few-shot correction database~~ — BUILT (2026-06-12, Milestone 3):** `corrections` table (migration 010) + keyword-overlap retrieval of the 5 most similar past corrections injected into every Groq/Gemini classification prompt. Verified to flip a misclassification after a single stored correction.
*   **~~Admin reply re-classification~~ — BUILT (2026-06-12, Milestone 3):** both admin-reply paths evaluate the reply against the assigned category via Groq and silently fix `category` (never status), logging the correction with source `admin_reply`.
*   **~~"Verify" accuracy function~~ — BUILT (2026-06-12, Milestone 4):** "Training Loop Verification" section in the Benchmark modal + `POST /api/verify` background job. Re-classifies human-reviewed messages with and without few-shot corrections (leave-one-out so a message never sees its own answer) and reports baseline vs trained accuracy, including a human-fixed-cases-only breakdown. This is the weekly number for the PRD success metric. Note: the `corrections` table is currently EMPTY (Milestone 3's test cleanup cascaded its rows) — the team needs to review real tickets in /train before this produces meaningful numbers. **Reality check (2026-06-13):** fewer than a handful of tickets have ever been reviewed in /train (the live DB shows 5 `corrections` rows), likely from automated testing rather than real human review. Training-loop verification is therefore meaningless until **20–30 real tickets are manually reviewed with full conversation context** (the /train page now provides a "Show full conversation" toggle and a "View on Telegram" deep link for exactly this). Separately, the benchmark cases (`benchmark-cases.ts`) contain **no Nigerian Pidgin examples** — a known gap (see §3 Pidgin) that limits how representative the accuracy numbers are.
*   **~~Automated status update bot~~ — BUILT (2026-06-12, Milestone 5), ships OFF:** dashboard status changes to Resolved / Escalated / Awaiting User reply to the user's original Telegram message with a fixed template, behind a `BOT_REPLIES_ENABLED` kill switch (default off) and a `BOT_REPLIES_DRY_RUN` mode (default on). Verified locally in dry-run: 18/18 suite assertions + kill-switch silence; all rails proven (dedup, rate limit, 7-day age guard, admin-ticket skip, group hard-rail, self-ingestion guard). Production rollout attempted 2026-06-12: Phase B (dry-run) passed, Phase C (live) BLOCKED — the session account is banned from sending in the group; feature parked — see section 7.
*   **Improved category taxonomy:** With granular Quidax-specific categories (e.g., distinguishing between "Withdrawal Issue" and "Transaction Dispute").
*   **PR-Based Development Workflow:** Enforcing strict PRs for all feature additions with mandatory plain-English documentation.

## 5. Milestone 3 known limitations (accepted by design, 2026-06-12)
*   **Few-shot similarity is naive keyword overlap** (lowercased words ≥4 chars, stopword-filtered, capped at 200 candidate corrections). No embeddings/semantic match — Pidgin spelling variants ("withdraw" vs "widthdraw" vs "remove my money") won't match each other. Good enough at current volume; revisit with embeddings if corrections grow past a few hundred.
*   **A wrong admin-reply correction propagates:** if the Groq audit misreads an admin reply and "corrects" a ticket wrongly, that wrong example is injected into future similar classifications until a human fixes the ticket again in /train (a later human_ui correction wins on recency in few-shot dedupe, and a human can re-correct the ticket there only if it has no human_ui row yet — otherwise fix the category from the main dashboard... note: there is no dashboard category editor yet, so the fix is deleting the bad corrections row in Supabase).
*   **Multiple admin replies on one ticket each fire `reclassifyFromAdminReply`** (observed live 2026-06-13): a ticket with two admin replies produced two `admin_reply` corrections seconds apart that contradicted each other (GeneralQuestion→App Bug then App Bug→GeneralQuestion, net zero). Benign at current volume — few-shot dedupe is newest-wins per `message_text`, so the pool effectively keeps only the latest verdict — but it does write opposing examples and doubles the Groq audit calls per ticket. Revisit if admin-reply volume grows (e.g. only re-audit when the category would actually change, or debounce per ticket).
*   **Tickets corrected via admin_reply are skipped by /train** (any corrections row counts as "reviewed"). Deliberate: the human loop should focus on tickets no one has looked at.
*   **`/api/eval` benchmark does NOT use few-shot corrections** — it stays a raw-model baseline so accuracy numbers are comparable across time. The live pipeline and `/api/test-message` DO use them.
*   **Re-classification only touches `category`** — urgency/product_area/summary are never revised by an admin reply, and status is never changed by the re-classifier.
*   **The 5-10s classification race now has a 12s retry** in `reclassifyFromAdminReply` (waits once for the classifier to settle, then skips if still pending). The status half of that race (classifier overwriting "In Review") was fixed in Milestone 4 (2026-06-12) — see section 3.

## 6. FULL SYSTEM AUDIT (2026-06-12 evening) — root causes confirmed, fix phase queued

Audited against the code, the live DB, and Railway logs. No fixes applied yet. Planned fix order (one commit per item, evidence required):

1. **~~(Critical) The live Telegram listener ingests NOTHING — AutoFetch is the only working ingestion path.~~ — FIXED (2026-06-13, Fix 6, commit `1a7130d`).** Proof of the bug: minimum message-timestamp→ingested_at lag was 13.6s / 29s / 368s / 134s across Jun 9–12 (live ingestion would be 1–5s), and Railway logs 19:04–23:14Z show zero "Live message received" lines while the sweeps classified ~16 tickets. The NewMessage handler stamped `lastMessageReceivedAt` BEFORE the target-group check, so the 30-min watchdog never reconnected while DMs/other chats kept arriving (corroborated live 2026-06-13: production `/api/health` reported a `lastMessageReceivedAt` of "now" despite the group listener being dead — the clock was being warmed by non-group traffic). Fix: the watchdog clock is now stamped ONLY when a message is in the target group; group membership is decided by exact resolved channel id (with the old username/title heuristic kept only as a fallback during the brief pre-resolution startup window, so nothing is dropped meanwhile); and the live log records `chatId`/`matchedBy` for production confirmation. The decision logic is the pure module `listener-health.ts` (`isMessageInTargetGroup`, `matchPath`, `shouldReconnect`, 16 unit tests). The live NewMessage leg cannot run on the no-telegram launcher (`tlClient` null); on the `e87cb89` production deploy (2026-06-13) channel-id resolution was confirmed live (`Resolved target group OfficialQuidaxCommunity to channel id 2129818880`), so the exact-id match path — not the fuzzy fallback — is active; awaiting one live `Live message received` line (`matchedBy:"channelId"`) for full verification. **Verification attempt 1 (2026-06-13) — watchdog half CONFIRMED, live-delivery half still pending:** Railway logs show the watchdog correctly tripping after 30 min of group silence (`No Telegram messages received for 30 minutes` → `Force reconnecting` → `Reconnected to Telegram` at 15:08Z) — exactly the Fix 6 behavior, proving the clock is no longer warmed by non-group traffic (pre-fix it never tripped). `/api/health` then showed `lastMessageReceivedAt` advancing to 15:12:35Z post-reconnect, suggesting live group delivery resumed. BUT no explicit `Live message received`/`matchedBy:"channelId"` line was captured, and the ticket ingested at 15:08 followed the AutoFetch "Periodic check" — so the definitive live-listener-delivers proof is still outstanding. (The sweep safety net remains: latest-20-messages / 15 min, processed oldest-first since Fix 3.) **DEEPER ROOT CAUSE FOUND — Fix 10 (2026-06-13, commit `14abd74`):** Fix 6 corrected the watchdog but the live listener STILL delivered nothing, because the watchdog only reconnects — it never addressed WHY messages weren't arriving. Fresh proof on the `22d71ba` build (live-DB lag query, 25 newest messages): min lag **185.8s**, every `ingested_at` clustered into 15-min AutoFetch batches; the same client's `getEntity`/`getMessages` worked fine, localizing the fault to the update PUSH stream. GramJS 2.26.x is push-only — `catchUp()` is a no-op and there is no `getDifference`/`getChannelDifference` recovery (`node_modules/telegram/client/updates.js`) — so a `StringSession` client must be primed once with `client.getDialogs()` after connecting or Telegram never pushes the supergroup's `UpdateNewChannelMessage`. The code never primed. Fix 10 adds a guarded `primeChannelUpdates()` (`getDialogs`) at startup and after every watchdog reconnect (pure module `dialog-priming.ts`, `findTargetInDialogs`, 9 tests) and logs whether the target group is present in the dialog list — which is ALSO the definitive membership probe the audit couldn't run without a second session: **if the group is ABSENT, the account is banned/not a member (Milestone 5 `USER_BANNED_IN_CHANNEL`) and no code fix can revive live delivery — a Telegram-account swap is required.** Deployed; awaiting the production log verdict (present+`Live message received` → CLOSED, or `TARGET GROUP IS ABSENT` → account swap). Also note: `/api/health.lastMessageReceivedAt` is initialised to BOOT time and never reset by the watchdog, so a freshly-deployed-but-dead listener reads "healthy" for up to 30 min — interpret it together with DB ingest lag, not alone.
2. **(Critical) Messages are silently dropped forever:** a user message that QUOTES another message and matches no active ticket hits `return null` (server.ts:1728) WITHOUT being inserted into `messages` — and every later sweep re-drops it identically. This ate the 16:39 support issue and the 16:44 admin quote (telegram IDs ~139581–139582, verified absent from the DB). Media-only messages are skipped (`!msg.text`, server.ts:2105); <5-char messages throw into the silent catch. Fix: every non-duplicate group message must persist a `messages` row before any attach/drop decision, and every skip must be logged.
3. **(Critical) AutoFetch iterates GramJS results newest-first** (server.ts:2100-2104): within a sweep, replies are always processed BEFORE their parents (quoted admin attach structurally fails) and raw_text blocks append in reverse chronological order — the dashboard then shows auto-resolve before the admin reply (proven: ticket 6784c670, auto-resolve block at char 174, [ADMIN_REPLY] at 272; frontend rendering is faithful, App.tsx:1383-1402). Fix: process oldest-first; separate previewed repair script for historical tickets.
4. **(Critical) Cross-chat edit/delete corruption:** the Raw edit/delete handlers (server.ts:2179-2247) have NO group filter, and Telegram message IDs are per-chat. Proven in Railway logs: "Message 221507 edited - updated in DB" plus 55659/55660 — IDs far outside the group's ~139xxx sequence overwrote our rows. The delete handler can silently Dismiss real tickets when an unrelated chat deletes a colliding ID. Fix: filter both handlers to the target group's peer/channel id.
5. **(Critical) Real issues are auto-dismissed:** "General Question" is in the auto-dismiss list (server.ts:1948-1962) AND is the default for every classification failure (Zod `.catch` :261, CLASSIFICATION_FALLBACK :269, normalizeCategory fallback :292) — any message the model fumbles becomes invisible. **Approved policy:** dismiss only pre-filtered + Spam/Irrelevant + Praise; "General Question" → Open/Low (Issues Only view already excludes the category); failed/fallback classifications must never be dismissible (flag them, keep Open).
6. **~~(High) Benchmark modal broken in prod~~ — FIXED (2026-06-13, Fix 7, commit `c8b2ea9`).** Was: `benchmark_cases.json` is gitignored → never deployed ("Could not load benchmark_cases.json" in Railway logs); `/api/eval` returned `total: 0` and NaN→null accuracies; the modal rendered a bare "%" + "Needs tuning" with an empty table and a fake "20 test messages" label; the eval loop lacked the 2.1s Groq spacing. Fix: the 12 cases now live in a committed TS module (`benchmark-cases.ts`) that bundles into `dist/server.mjs`, so they always deploy; the eval loop spaces Groq calls 2.1s apart (matching the verify loop); the accuracy math returns `null` not NaN when there are no cases; the modal shows a neutral "No benchmark cases available" empty state instead of a bare "%"; the loading text no longer claims a fixed count; and the `eval.ts` CLI reads the same shared module (single source of truth). Verified live on the `c8b2ea9` deploy: production `/api/eval` returned `total: 12` with real non-null numbers (category 100%, urgency 75%, overall 75%) and a 12-row results table. Still-open gap (deliberately not fixed here): the 12 cases are Quidax-representative English with **no Nigerian Pidgin** — see §3 Pidgin item.
7. **~~(High) Gemini suggested replies failing across the board since ~21:34Z Jun 12~~ — FIXED (2026-06-13, Fix 8, commit `7a09b15`).** Was: every sweep, attempt 1 failed instantly and the gemini circuit opened (pattern: daily free-tier quota exhaustion; the 15-min repair sweep kept re-burning it because `isRetryableLLMError` treats a 429/"resource exhausted" as retryable, so each ticket fired 3 real Gemini calls and tripped the breaker every cycle). The retry log line carried only `e.message`. Fix: (a) failure logs now emit the FULL error detail via `describeLLMError` (status, statusText, errorDetails) so the failure mode is confirmed from logs, not guessed; (b) a quota-aware cooldown — when `isQuotaExhaustedError` (429 / RESOURCE_EXHAUSTED / "quota", deliberately NOT a transient 503/overload) fires, `generateSuggestedReply` arms a 60-min cooldown and stops retrying immediately (1 failing call, not 3); during the cooldown the repair sweep skips with one log line and the live pipeline never calls Gemini, so the quota is no longer re-burned and the shared `geminiBreaker` stays closed for other Gemini work. A transient overload still uses the existing short-backoff retry. Quota detection + error description live in the pure module `gemini-quota.ts` (9 unit tests). **Real-log confirmation of the ACTUAL error detail is a post-deploy step** (Railway logs after the next sweep) — the cooldown only engages on a true quota error, so if production logs instead show e.g. a 404 from the `gemini-3.5-flash` reply model name, behavior is unchanged and a model change becomes the follow-up. **Verification attempt 1 (2026-06-13) — INCONCLUSIVE, still pending:** the Railway logs reviewed were from the pre–Fix-8 build (server.mjs 118.3kb, booted 14:38:07Z, old logging) and contained NO `ReplyRepair`/Gemini-error line at all — the sweep returns silently when 0 recent active null-reply tickets qualify, so nothing was captured. The `gemini` breaker is currently CLOSED in `/api/health` (the failure may have abated as the daily quota reset). Still need a sweep on the `7a09b15` build during a window with a qualifying ticket to capture the `describeLLMError` output and confirm quota vs. 404.
8. **~~(High) Corrections pipeline starved, not broken~~ — VERIFIED UNBLOCKED in production (2026-06-13).** Was: `corrections` had 0 rows; only 1 [ADMIN_REPLY] attachment since the feature shipped Jun 12, because quoted parents were never ingested (items 1–3) — admin replies became standalone Resolved tickets instead. The starvation was a SYMPTOM of items 1–3 (silent drops, AutoFetch ordering), item 5 (missing quoted parent), and item 1 (dead live listener), all FIXED (Fixes 1, 3, 5, 6); `reclassifyFromAdminReply` (server.ts:1081-1185) was always code-correct. **Live confirmation (2026-06-13, direct DB query):** `corrections` now holds 5 rows, all created 2026-06-13, of which 4 are `correction_source = admin_reply` from a real admin (`corrected_by` hash `7b08fcbe362a78e6`, not `sys_admin`) on REAL ingested group tickets — telegram_message_id 139613/139614/139620 (genuine ~139xxx group sequence), e.g. ticket `232beeb4` ("How do I get quidax app on iPhone") with two `[ADMIN_REPLY]` blocks and `first_admin_reply_at` stamped, now `In Review`. So real admin replies attach to real parent tickets and fire `reclassifyFromAdminReply` end-to-end. **Observation (logged in §5, not a regression):** a ticket with TWO admin replies generates TWO `admin_reply` corrections (one per reply), which can contradict each other (~3s apart, e.g. GeneralQuestion→App Bug then App Bug→GeneralQuestion, net zero) — benign because few-shot dedupe is newest-wins per message_text, but it does write opposing examples into the pool. ~~Also: the quoted-admin branch never retro-ingests a missing parent.~~ **Retro-ingest FIXED (2026-06-13, Fix 5, commit `e848d5f`)** — see the Fix 5 entry below.
### Fix progress (one commit per item)
*   **Fix 1 — item 2 DONE (commit `586dedf`):** every non-duplicate message is persisted before any attach/drop decision; skips are logged.
*   **Fix 2 — item 4 DONE (commit `048cf40`):** edit/delete handlers filtered to the resolved target-group channel id (`telegram-guards.ts`, 9 tests).
*   **Fix 3 — item 3 DONE (commit `990e1cd`):** AutoFetch processes oldest-first.
*   **Fix 4 — item 5 DONE (commit `0494a1a`):** only spam/praise auto-dismissed; "General Question" stays Open/Low; failed classifications flagged `[NEEDS REVIEW]`, never dismissed (`classification-policy.ts`, 14 tests).
*   **Fix 5 — item 8 (retro-ingest) DONE (commit `e848d5f`, 2026-06-13):** when an admin reply quotes a message that has no parent ticket in our DB, the quoted-admin branch now fetches that message from Telegram by id (`client.getMessages(targetGroup, { ids: [id] })`), ingests it as a root ticket, then attaches the reply — instead of degrading to a standalone Resolved admin ticket and losing the thread. Orchestration lives in the pure module `quoted-parent.ts` (`recoverQuotedParent`, 5 unit tests) so it is testable without a live session; the Telegram fetch is injected and is a safe no-op when no session is connected (the no-telegram launcher). Composes with Fix 1 (the admin reply's own row is already persisted; the parent carries a different telegram id) and ingests the parent as a ROOT message (no `replyToMsgId`) so recovery can never recurse up a reply chain. The real fetch can't run on the local launcher, so it was proven by the unit tests plus a live-DB check of the attach-after-resolve path: a quoted admin reply to an existing user ticket attached correctly (Open→In Review, `[ADMIN_REPLY]` block, `first_admin_reply_at` stamped, 0 orphan tickets, both `messages` rows present once); test data cleaned up, `corrections` count returned to baseline (2).
*   **Fix 6 — item 1 DONE (commit `1a7130d`, 2026-06-13):** the watchdog "last message seen" clock is stamped only on TARGET-group messages (DMs/other chats no longer keep it warm, so a silently-dead group listener finally trips the 30-min reconnect); group membership is matched by exact resolved channel id, falling back to the old username/title heuristic only before that id resolves at startup. Pure module `listener-health.ts` (`isMessageInTargetGroup`, `matchPath`, `shouldReconnect`, 16 tests). Live log now records `chatId`/`matchedBy`. Channel-id resolution confirmed live on the `e87cb89` deploy (id 2129818880); awaiting one `Live message received` line for full verification.
*   **Fix 7 — item 6 DONE (commit `c8b2ea9`, 2026-06-13):** the 12 benchmark cases ship as a committed TS module (`benchmark-cases.ts`, bundled into dist instead of the gitignored `benchmark_cases.json`); the eval loop spaces Groq calls 2.1s apart; the accuracy math returns null (not NaN) with no cases; the modal degrades to a "No benchmark cases available" empty state and drops the fake "20 test messages" label; `eval.ts` CLI reads the same module. Structural guard: `tests/benchmark-cases.test.ts` (6 tests — 12 cases, unique ids, valid categories/urgencies). Verified live on the `c8b2ea9` deploy: `/api/eval` returned `total: 12`, category 100% / urgency 75% / overall 75%, 12-row table. No Pidgin in the set (known gap, §3).
*   **Fix 8 — item 7 DONE (commit `7a09b15`, 2026-06-13):** Gemini suggested replies stopped re-burning an exhausted quota. Failure logs now emit the full error detail (`describeLLMError`: status/statusText/errorDetails) instead of a bare message; a quota / 429 / RESOURCE_EXHAUSTED error (`isQuotaExhaustedError`, NOT a transient 503/overload) arms a 60-min cooldown so `generateSuggestedReply` stops after 1 failing call (not 3), the 15-min repair sweep skips with one log line, and the live pipeline does not call Gemini — keeping the shared `geminiBreaker` closed. Pure module `gemini-quota.ts` (9 tests). Real-log confirmation of the actual error detail is a post-deploy step (cooldown only engages on a true quota error; safe if the real cause turns out to be the `gemini-3.5-flash` model name → a 404, in which case a model change is the follow-up). Also marked item 8 resolved-pending-production-observation (it was a symptom of items 1–6, now all fixed).
*   **Verification pass 1 (2026-06-13):** **Item 8 VERIFIED unblocked** by direct DB query — 5 `corrections` rows all created Jun 13, 4 from real `admin_reply` on real group tickets (telegram ids 139613/139614/139620). **Item 1 (Fix 6):** watchdog-trip half confirmed from logs (correctly reconnected after 30-min group silence) + `/api/health` clock advanced post-reconnect; the explicit `Live message received`/`matchedBy:"channelId"` line is still outstanding. **Item 7 (Fix 8):** PARTIALLY RESOLVED from the `0354351` build Railway logs — the server is running on the Fix 8 build (server.mjs 120.0kb, deploy 17:12Z), the `generateSuggestedReply attempt 1/3 failed - retrying with backoff` log lines ARE using Fix 8 formatting (detailed LLMErrorDetail logged as metadata — the error detail is in the structured log but not rendered in the Railway plain-text view), and the breaker OPENED at 17:13:22 then CLOSED at 17:15:05 with a successful repair ("Backfilled suggested reply for ticket bcfda164") — the open→close→repair cycle is consistent with a TRANSIENT 5xx/overload (not a quota 429, which would have logged `cooling down for 60min` and never recovered in 2 min). This rules out the 404 model-name bug. The literal HTTP status number is in the structured metadata field; to read it, expand the `Gemini` log line's metadata in the Railway log viewer. Practically confirmed: Fix 8 is working — the quota-cooldown path did not engage, which means Gemini was in a transient overload state, not a daily quota exhaustion. The fix protects against the daily quota case, which will be confirmed the next time quota exhaustion occurs.
*   **Fix 9 — §3 Pidgin DONE (commit `0354351`, 2026-06-13):** Nigerian Pidgin English classification gap closed — see §3 entry above for full detail. Benchmark extended to 18 cases (+ 6 Pidgin). 71/71 tests pass. /api/eval now measures Pidgin accuracy as a raw-model baseline. **LIVE-CONFIRMED on the `22d71ba` production deploy (2026-06-13):** `GET /api/eval` → `total:18`, category 100% / urgency 94% / overall 94%; all 6 Pidgin cases (ids 13–18) correct on both category and urgency. This is the production confirmation of Fix 9 (no remaining local-only caveat — unlike the live-log items for Fix 6/8, /api/eval exercises the same prompt the live pipeline uses).
*   **Fix 10 — item 1 (deeper root cause) DONE (commit `14abd74`, 2026-06-13):** Fix 6 fixed the watchdog but the live listener still delivered nothing; Fix 10 found why — GramJS 2.26.x is push-only and the `StringSession` client was never primed with `getDialogs()`, so the supergroup's `UpdateNewChannelMessage` was never pushed (proof: min DB ingest lag 185.8s, all AutoFetch-batched, while the same client's history reads worked). Adds a guarded `primeChannelUpdates()` (getDialogs) at startup + after each watchdog reconnect, and a membership-revealing log line. Pure module `dialog-priming.ts` (`findTargetInDialogs`, 9 tests, reuses `isMessageInTargetGroup`/`matchPath`). tsc clean, 80/80 tests, build OK + bundled, local no-telegram boot clean. Live leg unverified until production (tlClient null locally) — the next deploy's priming log gives the verdict, INCLUDING whether the account is even a member.
*   **Still queued:** none from §6 items 1–8 (all addressed in code). §3 Pidgin gap closed (Fix 9) and **live-confirmed in production** (`/api/eval` total:18, Pidgin 6/6 cat + 6/6 urg on the `22d71ba` deploy). Remaining is **production-log confirmation only**: Fix 10/6 (after deploy, expect either `target group present in dialogs` + the first `Live message received`/`matchedBy:"channelId"` line + sub-5s ingest lag → item 1 CLOSED; OR `TARGET GROUP IS ABSENT` → the account is banned/not a member and a Telegram-account swap is the remaining non-code fix) and Fix 8 (Gemini error detail, practically resolved per verification pass 1).

9. **(Verified OK)** /train works in production (SPA 200, /api/train/next returned a real ticket, 685 reviewable, POST path untested by design); KPI cards are honest (0 active in 24h is the true DB state; the "Escalated" status has never been set on any ticket EVER — it is manual-only, no code path sets it); shouldProcessMessage is NOT inverted (pre-filter rejects still become Dismissed tickets — a message absent from All Messages was never ingested at all); the date filters are correct.

## 7. Milestone 5 (status update bot) — rollout state & v1 limitations (2026-06-12)
*   **PARKED — BLOCKED BY TELEGRAM, NOT BY CODE (2026-06-12 evening).** Phase B passed in production: with `BOT_REPLIES_ENABLED=true` + dry-run, a real status change produced exactly one correct `bot_replies` row (right target message, right group, exact template, all rails held). Phase C failed at the first live send: `400: USER_BANNED_IN_CHANNEL` — the GramJS session account is banned/muted from SENDING in OfficialQuidaxCommunity (reading/ingestion is unaffected). Nothing was posted to the group; the failure was recorded cleanly. `BOT_REPLIES_ENABLED` is back to false on Railway and the feature is parked until the account is unmuted or replaced with one that has send rights (a different account means a new session string — explicit instruction required). **Dry-run can structurally never catch this class of failure** (it never calls Telegram). One burned pair exists: the failed row blocks (ticket `0c72808f-1112-4dd7-aaa1-ed5ad2f9e938`, "Awaiting User") forever — delete that row before any retry.
*   **Failed sends are never auto-retried** (deliberate v1): the claim row in `bot_replies` stays `result='failed'` and permanently blocks that (ticket, status) pair — visible in logs and the table. If a retry is ever wanted, delete the failed row manually; an automated repair could later join the 15-minute sweep.
*   **Once per (ticket, status), EVER** (product decision): resolve → reopen → resolve again posts nothing the second time. Status flapping can never spam the group.
*   **Tickets older than 7 days never get a reply** — bulk backlog cleanup is silent by design. The bound is a code constant (`BOT_REPLY_MAX_TICKET_AGE_DAYS`).
*   **Replies come from the session user account, not a bot account**, with a deliberately neutral voice (no "Quidax Support" signature) since the account is not officially sanctioned to speak for Quidax.
*   **The dashboard gets no feedback** about whether a bot reply was sent/skipped — outcomes live in server logs and the `bot_replies` table only (v1 scope cut).

## 8. Future Enhancements
*   **Conversation threading:** PulseDesk currently treats each Telegram message as a potential individual ticket. Real support conversations often span 10–20 back-and-forth messages between a user and admin without quoting. A future version should detect conversation threads (same user, same time window) and group them as a single ticket with a full thread view. This would significantly improve classification accuracy since the AI would see full context rather than isolated messages.
